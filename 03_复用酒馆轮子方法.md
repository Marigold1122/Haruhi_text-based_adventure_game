# 03. 复用酒馆轮子方法

## 总体思路

我们不从零实现完整 AI 编排平台。

可以复用 SillyTavern/酒馆的成熟机制：

- 正则
- 世界书
- 预设 / 破限
- 角色卡

项目中的独立前端负责展示和交互；酒馆负责上下文、设定注入、模型约束和输出协议稳定。

一句话：

> 酒馆做 AI 运行时，我们做互动小说前端。

## 要复用的四个轮子

### 1. 角色卡

角色卡不一定只代表一个角色。这里建议做成“作品级入口卡”。

角色卡负责：

- 项目总设定
- 开场消息
- 故事风格
- 玩家身份
- 主要角色列表
- 输出协议
- 绑定世界书
- 绑定正则脚本

角色卡里可以写：

```text
你是一个校园异常文字冒险的叙事引擎。
你需要根据玩家选择或自由输入，推进一轮事件。
你不能替玩家做重大决定。
你不能一次性揭示全部真相。
你必须输出 <event_json> 块。
```

参考之前提取的 CLANNAD / Kanon 卡：

- 它们不是普通单角色卡。
- 它们更像作品级卡。
- 卡里包含世界书、输出协议、前端 regex 和资源映射。

我们的版本可以更轻：

- 不做复杂 HTML 前端卡。
- 先做稳定协议。
- 后续再把前端嵌入 regex。

### 2. 世界书

世界书负责让 AI 不散。

建议世界书分组：

```text
核心规则
地点
主要人物
异常规则
事件触发
语气风格
禁止事项
```

核心规则条目：

- 不读心。
- 不替玩家行动。
- 每轮只推进一个小事件。
- 不一次性揭示真相。
- 保持校园异常风格。
- 输出必须可解析。

地点条目：

- 社团活动室
- 教学楼走廊
- 校门口
- 电脑教室
- 图书馆
- 天台

人物条目：

- 团长型角色：主动、兴奋、推动事件。
- 文静观察者：冷静、信息密度高、像在记录异常。
- 学姐型角色：紧张、温柔、制造日常感。
- 转学生型角色：微笑、含糊、知道更多但不直说。
- 玩家：行动由用户决定。

异常规则条目：

- 异常等级 0 到 100。
- 0 到 30：轻微违和。
- 31 到 60：明确异常。
- 61 到 85：现实开始不稳定。
- 86 到 100：进入危机或结局。

事件条目：

- 旧电脑无输入启动。
- 广播播放不存在的社团通知。
- 活动室门牌文字变化。
- 同一天的黄昏重复出现。
- 社刊空白页浮现文字。

### 3. 预设 / 破限

这里的“破限”不是让模型乱来，而是稳定模型行为。

目标：

- 避免模型变成解释型助手。
- 避免模型拒绝普通虚构叙事。
- 避免模型输出 Markdown 分析。
- 避免模型跳出故事世界。
- 强制模型按协议返回。

预设应该强调：

```text
你不是聊天助手。
你是互动小说叙事引擎。
你只根据当前状态和玩家行动推进下一轮事件。
你必须只输出一个 <event_json> 块。
<event_json> 内必须是合法 JSON。
不要解释，不要总结，不要输出 Markdown。
```

玩家边界：

```text
不要替玩家说话。
不要替玩家做重大选择。
可以描写玩家行动造成的合理结果。
如果玩家行动不明确，用事件反馈引导，而不是擅自改写玩家意图。
```

剧情边界：

```text
不要一次性揭示所有谜底。
不要突然跳到大结局。
不要引入过多新人物。
每轮只推进一个事件点。
```

### 4. 正则

正则负责从 AI 回复中捕获结构化内容。

推荐输出：

```text
<event_json>
{
  "eventTitle": "旧电脑的异常启动",
  "scene": "社团活动室",
  "time": "放学后",
  "mood": "日常中混入不安",
  "narration": "你走到角落的旧电脑前，屏幕在你触碰鼠标之前自己亮了起来。",
  "dialogue": [
    {
      "speaker": "团长",
      "mood": "兴奋",
      "text": "看吧！我就说今天一定会发生什么！"
    }
  ],
  "stateChanges": {
    "anomalyLevelDelta": 8,
    "playerStressDelta": 2,
    "addClues": ["旧电脑无输入启动"],
    "addFlags": ["computer_awake"],
    "memberUpdates": [
      { "name": "团长", "status": "兴奋" }
    ]
  },
  "choices": [
    "询问团长是不是提前安排了机关",
    "让文静少女检查电脑日志",
    "假装没看到，观察其他人的反应"
  ]
}
</event_json>
```

正则：

```regex
<event_json>([\s\S]*?)<\/event_json>
```

两种使用方式：

#### 方式 A：只捕获 JSON

正则捕获 JSON，前端或脚本解析后更新 UI。

适合 MVP，因为稳定。

#### 方式 B：替换成前端卡

正则直接把 AI 回复替换成 HTML/CSS/JS 前端卡。

类似之前 CLANNAD / Kanon 卡的方式。

适合后续增强，不建议第一天就做复杂。

## 推荐接入步骤

### 第一步：独立前端先跑通

先不接酒馆。

- 用 mock 数据实现 3 到 5 轮剧情。
- 做好选项和自由输入。
- 做好状态变化。
- 做好 fallback。

### 第二步：协议稳定

让 mock 数据和未来 AI 输出使用同一套结构。

- `eventTitle`
- `narration`
- `dialogue`
- `stateChanges`
- `choices`

### 第三步：酒馆里验证输出

在酒馆里创建角色卡和世界书草案。

测试模型是否能稳定输出：

```text
<event_json>
{ ... }
</event_json>
```

### 第四步：正则捕获 demo

先只做捕获，不急着做复杂前端替换。

验证：

- 是否能捕获完整 JSON。
- 是否会吞掉多余文本。
- 模型是否会输出多个块。
- JSON 是否能 parse。

### 第五步：前端联动

把酒馆输出接到前端展示。

黑客松里可以用最轻方式：

- 手动复制模型输出到前端测试。
- 或让前端预留“粘贴 event_json”调试入口。
- 或做一个最小 regex 前端卡。

## 角色卡草案结构

```text
name: 校园异常社团事件簿

description:
这是一个 AI 驱动的校园异常文字冒险。玩家是社团成员之一，需要通过选择和自由行动参与异常事件调查。

first_mes:
<event_json>
{ 开场事件 JSON }
</event_json>

system / creator notes:
- 你是互动小说叙事引擎。
- 每轮只推进一个事件。
- 不替玩家行动。
- 输出唯一 event_json 块。

extensions:
- 绑定世界书名称
- 可选 UI 主题信息
- 可选初始状态
```

## 世界书草案结构

```text
Entry: 输出协议
Keys: 协议, JSON, event_json
Content: 每次回复必须输出唯一 <event_json> 块，块内为合法 JSON。

Entry: 核心规则
Keys: 规则, 边界, 玩家
Content: 不读心，不替玩家行动，不一次性揭示真相。

Entry: 社团活动室
Keys: 社团活动室, 活动室, SOS
Content: 放学后主要场景，旧电脑、长桌、窗边、社刊、门牌。

Entry: 异常等级
Keys: 异常等级, 异常, anomaly
Content: 0-100，越高现实越不稳定。
```

## 风险和边界

不要两天内做：

- 完整酒馆扩展。
- 完整 PNG 角色卡写回工具。
- 大型世界书管理 UI。
- 很复杂的 regex 前端卡。
- 多模型适配。

两天内只做：

- 一份角色卡草案。
- 一份世界书草案。
- 一个正则捕获 demo。
- 一个独立前端原型。
- 一套稳定输出协议。


