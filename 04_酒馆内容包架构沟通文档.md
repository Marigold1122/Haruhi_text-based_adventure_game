# 04. 酒馆内容包架构沟通文档

这份文档是给队友沟通用的

目前项目已经有一个能运行的 React 游戏壳，也已经把很多 SillyTavern（下文简称“酒馆”）里的概念用 TypeScript 做了出来，比如角色卡、世界书、Preset、Prompt Router、`<event_json>` 输出协议。

现在需要讨论的是：以后 LLM 内容的“权威来源”到底应该放在哪里。

换句话说：

- 是把酒馆的概念翻译成项目里的 TS 文件，然后由代码来管理一切？
- 还是尽量保留酒馆原本的内容组织方式，让项目读取“角色卡 / 世界书 / 预设 / 正则”这些内容包？

这两个方向都能做，但它们的工作流不一样。如果混在一起，后面会很容易出现 prompt 互相冲突、调试困难、导入酒馆预设却效果不像酒馆的问题。

## 先说结论

我希望项目最终更接近这个结构（不造轮子）：

```text
游戏前端壳子
  负责：界面、按钮、存档、状态展示、回合推进、解析 event_json

酒馆式内容包
  负责：角色卡、世界书、文风预设、输出规则、正则或后处理
```

也就是说，用户最终看到的是一个普通网页游戏。

但开发和调试时，我们可以像在酒馆里一样写卡、调预设、调世界书。等调满意后，再把这套内容包隐藏在项目里，用户不需要看见它。

这个方向不是说现在的代码都错了，而是说：现在代码里有一些 prompt 内容写得太“硬”，后续如果要导入酒馆预设，容易打架。

## 现在项目大概是什么思路

现在项目更像是：

```text
项目自己实现一套“类酒馆运行时”
```

也就是：

- React 前端负责游戏 UI。
- TypeScript 里定义角色卡格式。
- TypeScript 里定义世界书格式。
- TypeScript 里定义 Preset。
- TypeScript 里定义时间表。
- TypeScript 里定义身份规则。
- `promptRouter` 把这些东西拼成给 LLM 的 messages。
- LLM 输出 `<event_json>`。
- 前端解析 JSON 后更新游戏状态。

这个思路的优点是：

- 不依赖真正的酒馆。
- 项目可以作为一个独立网页直接运行。
- 数据结构清楚，方便程序员维护。
- 黑客松或 demo 阶段能快速闭环。

但它的问题是：

- 很多 LLM 行为被写死在 TS 文件里。
- 文风、世界观、输出协议、身份边界、时间表分散在多个地方。
- 如果再导入酒馆预设，预设会和 TS 里已有 prompt 互相影响。
- 内容作者想像在酒馆里那样调卡，会比较难。

## 我说的“酒馆式内容包”是什么意思

酒馆里一般有几类东西：

### 1. 角色卡

角色卡不是只能代表单个角色。

它也可以是一张“作品入口卡”或“世界入口卡”。

比如参考卡 `Clannad_v3.2.png` 和 `keyKanon_v1.5.png` 就不是普通单角色卡。它们更像是：

```text
一个作品级入口
  包含核心原则
  包含世界书
  包含人物条目
  包含输出协议
  包含正则和前端渲染信息
```

它们的结构大概是：

- `description` 写核心规则，比如主观视角、信息有限、不能读心。
- `character_book.entries` 写世界书条目。
- `extensions.regex_scripts` 写正则或前端处理。
- `extensions.world` 指向绑定的世界。

这是一种很“酒馆”的写法。

### 2. 世界书 / Character Book

世界书负责放事实和设定。

例如：

- 世界观规则
- 地点
- 人物
- 事件
- 组织
- 禁止事项
- 根据关键词触发的条目
- 根据身份或事件链触发的条目

世界书不应该主要负责“文风”。

它更像资料库，告诉模型“这个世界里有什么是真的”。

### 3. Preset

Preset 更像模型的写作方式和调用方式配置。

它可能包括：

- temperature
- top_p
- max tokens
- prompt 顺序
- 文风强化
- 模型行为约束
- jailbreak 或格式强化

也就是说，Preset 更适合管“怎么写”，世界书更适合管“写什么是真的”。

### 4. 正则 / 后处理

正则负责把模型输出转换成前端能展示的形式，或者抽取某些内容。

例如：

- 抽出 `<event_json>`
- 把 `<content>` 转换成 HTML 卡片
- 删除多余 tag
- 修正格式

我们项目目前最核心的是抽取 `<event_json>`，这个属于前端和 LLM 的接口契约。

## 现在冲突在哪里

当前项目里，很多层都在对 LLM 说“你应该这样写”。

比如：

### Preset 里有一部分规则

`src/data/presets/index.ts`

这里写了：

- 输出格式
- 主线机制
- 身份变化
- 节奏字段
- 文风要求
- 禁止事项

### 世界书里也有一部分规则

`src/data/lorebooks/hsuzumiya_lore.ts`

这里也写了：

- 原作硬事实
- 原作叙事风味
- 禁止事项
- 输出必须 `<event_json>`

### 随机角色生成里也有一部分规则

`src/lib/randomMode.ts`

这里让 LLM 生成玩家角色卡，并要求：

- `system_prompt` 要写 POV 规则
- `post_history_instructions` 要写输出必须 `<event_json>`
- scenario 要写起点和身份

### 时间表单独写在 TS 里

`src/data/worldTimeline.ts`

这里写了原作事件时间表。

但它本质上更像世界书条目，而不是 Preset 的一部分。

### 身份规则也单独写在 TS 里

`src/data/identityGuide.ts`

这里写了不同身份能知道什么、能参与什么。

它也更像动态世界书或运行时状态说明，而不是文风预设。

## 这会导致什么问题

如果我们把一个酒馆预设导入项目，比如：

`夏瑾 双鱼座 Beta 0.40.json`

它不会自动产生和放进酒馆一样的效果。

原因是：

这个 JSON 不是单纯一段 prompt。它是酒馆 Chat Completion preset，里面有：

- `prompts`
- `prompt_order`
- 采样参数
- 酒馆内置 marker，比如 `charDescription`、`worldInfoBefore`、`chatHistory`
- `extensions.regex_scripts`
- `extensions.SPreset`

酒馆会用自己的 Prompt Manager 按顺序处理它。

而我们项目现在有自己的 `promptRouter`。它并不完整复刻酒馆的 Prompt Manager。

所以现在如果只是把这个 JSON 放进项目里，最多只能“借用一部分内容”，不能等同于“像酒馆里一样运行”。

更麻烦的是：外部预设想管文风，但项目 TS 里已有的 Preset、世界书、角色卡 post instructions 也在管文风和输出格式。这样就会出现多个上级同时发命令。

## 一个更清晰的分工

我建议把职责分成五层。

### 第 1 层：游戏壳子

代码负责。

包括：

- 页面 UI
- 开始游戏
- 选择行动
- 存档
- 读取存档
- 状态面板
- 历史记录
- 调用 LLM
- 解析 `<event_json>`
- 根据 `stateChanges` 更新状态

这一层应该稳定，不应该频繁因为调文风而改代码。

### 第 2 层：Preset

酒馆预设负责。

包括：

- 文风
- 模型行为
- prompt 顺序
- 采样参数
- 一些通用写作约束

如果我们要使用 `夏瑾 双鱼座 Beta 0.40.json` 这种预设，就应该尽量让它在这一层发挥作用。

### 第 3 层：Campaign Card，也就是作品入口卡

这是我建议新增的概念。

它不是玩家角色卡，而是“凉宫春日 · 北口高校”这整个游戏世界的入口卡。

它可以像参考卡那样写：

```text
name: 凉宫春日 · 北口高校

description:
  作品核心原则
  主观视角
  信息有限
  不读心
  不替玩家决定
  世界遵循原作

character_book:
  原作硬事实
  北高地点
  SOS 团人物
  组织设定
  事件链
  时间表
  输出协议
```

这样，世界内容就从散落的 TS 文件，逐步变成一张可导入、可调试、可隐藏的作品卡。

### 第 4 层：Player Card，也就是玩家原创角色卡

玩家生成的原创角色卡只负责玩家本人。

它应该写：

- 玩家姓名
- 玩家背景
- 玩家性格
- 玩家身份
- 玩家知道什么
- 玩家和 SOS 团的关系
- 玩家有没有秘密身份

它不应该承担太多全局世界设定。

全局世界设定应该在 Campaign Card 或世界书里。

### 第 5 层：Dynamic World Info，也就是动态世界书

有些信息不是固定写死在卡里，而是跟当前游戏状态有关。

比如：

- 当前日期
- 当前场景
- 当前身份
- 当前事件链
- 当前关系
- 当前线索
- 最近发生过什么

这些可以由代码生成，但形式上应该当作“临时世界书条目”注入。

这样它们就不会和 Preset 混在一起。

## 时间表应该怎么处理

现在 `worldTimeline.ts` 是一个 TS 文件。

它的内容其实是：

```text
某天发生了什么原作事件
什么身份能看到
能不能剧透
是否关联事件链
```

这非常像世界书。

所以更合理的方向是：

把时间表改成动态世界书条目。

例如每条可以有：

```ts
{
  name: "入学典礼 · 凉宫春日的自我介绍宣言",
  content: "...",
  extensions: {
    date: "2002-04-08",
    visibility: "public",
    identity_gate: ["passerby", "fringe", "core", "anomaly", "observer"],
    window_before_days: 14,
    window_after_days: 14
  }
}
```

代码只负责根据当前日期筛选。

筛选出来后，它仍然作为 World Info 注入。

这样逻辑更统一：

```text
事实和设定都走世界书
文风和模型行为都走预设
当前状态走动态世界书
```

## 输出协议要不要放在预设里

这里要小心。

我们的前端必须解析 `<event_json>`，所以输出协议不能完全交给外部预设自由决定。

但它也不应该在很多地方重复写。

我建议只保留一个非常短的硬协议，作为最后兜底。

例如：

```text
本应用只能读取一个 <event_json>...</event_json> 块。
块内必须是合法 JSON。
字段包括 eventTitle、narration、dialogue、stateChanges、pace、timeAdvance、requiresChoice、choices。
块外不要输出其他文字。
```

这不是文风要求，而是程序接口。

文风交给预设。

世界观交给世界书。

接口契约由项目保留。

## 为什么不是直接把所有 TS 都删掉

不建议一上来就推翻重做。

现在项目里已经有很多有价值的东西：

- UI 已经能跑。
- `<event_json>` 解析已经有了。
- 世界状态结构已经有了。
- 存档已经有了。
- Prompt Router 已经有了雏形。
- 世界书类型也已经接近酒馆格式。

所以更稳的方式不是重写，而是逐步迁移职责。

## 建议的迁移步骤

### 第一步：先统一目标

先决定一句话：

```text
LLM 内容的权威来源，未来尽量是酒馆式内容包，而不是散落在 TS 文件里的硬编码 prompt。
```

如果大家同意这句话，后面的代码调整才有方向。

### 第二步：让 Prompt Router 支持角色卡自带 character_book

当前类型里已经有：

`CharacterCardV2.data.character_book`

但现在 Prompt Router 主要吃外部 `lorebook`。

可以先让它合并：

```text
Campaign Card character_book
Player Card character_book
Runtime dynamic world info
```

这样就能更接近酒馆的写法。

### 第三步：做一张 Campaign Card

把现在散在 TS 里的凉宫世界设定，先整理成一张作品入口卡。

不用一口气迁完。

可以先迁：

- 原作硬事实
- 输出协议
- 主要人物
- 北高地点
- 时间表

### 第四步：把 worldTimeline 改成动态世界书

代码仍然可以负责“按日期筛选”。

但筛选结果以 World Info 的形式进入 prompt。

### 第五步：Preset 只保留轻量适配

项目内置 Preset 不再写大量文风。

它只做两件事：

- 告诉模型当前是 daily / encounter / supernatural 这类节奏
- 给出最小接口契约

真正的文风由导入预设决定。

### 第六步：做酒馆预设适配器

这个适配器读取 `夏瑾 双鱼座 Beta 0.40.json` 这类文件。

它需要处理：

- `prompts`
- `prompt_order`
- 启用状态
- 采样参数
- marker，比如 worldInfoBefore、charDescription、chatHistory
- regex_scripts 是否启用

这一步做完后，外部预设才会比较接近酒馆里的效果。

## 对队友说的重点

这不是说“你现在写的架构错了”。

现在的架构是一个 code-first 的实现：

```text
代码是主系统，酒馆概念被翻译成代码数据。
```

我想要的是 content-pack-first：

```text
酒馆式内容包是主系统，代码负责加载、运行和隐藏它。
```

这两个方向都能跑。

但我们最好不要混成：

```text
代码里写一套 prompt
导入预设又写一套 prompt
角色卡里再写一套 prompt
世界书里还写一套 prompt
```

这样后面会很难调。

## 最后想和队友确认的几个问题

### 问题 1：我们希望长期内容怎么维护？

选项 A：

以后主要改 TS 文件。

选项 B：

以后主要改酒馆卡、世界书、预设，再导入项目。

我倾向选 B。

### 问题 2：项目是否要尽量兼容酒馆格式？

如果要，就应该优先支持：

- PNG 角色卡读取
- `data.character_book`
- preset `prompts + prompt_order`
- `extensions.regex_scripts`

### 问题 3：文风到底由谁控制？

我建议：

```text
文风由外部预设控制。
世界事实由世界书控制。
玩家身份由玩家卡和动态世界书控制。
前端接口由项目的最小 Output Contract 控制。
```

### 问题 4：时间表属于代码还是世界书？

我建议：

```text
时间表属于世界书，只是由代码按日期动态激活。
```

## 一句话总结

现在项目已经有了“像酒馆”的结构，但还不是“运行酒馆内容包”的结构。

我希望后续把它调整成：

```text
一个游戏壳子 + 一个隐藏的酒馆式内容包
```

这样我们可以先在酒馆式卡片和预设里把效果调满意，再把它包装进网页游戏。用户不需要理解这些内部内容，只需要玩游戏。

