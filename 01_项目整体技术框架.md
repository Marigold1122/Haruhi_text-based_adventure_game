# 01. 项目整体技术框架

## 定位

本项目是一个 **AI 驱动的校园异常文字冒险 / 交互小说原型**。

它不是完整 galgame，也不是普通聊天机器人。核心体验是：

> 玩家阅读事件，通过固定选项或自由输入行动参与故事，AI 根据当前状态生成下一轮事件，前端把结果呈现成可玩的互动小说界面。

## 产品形态

我们最终要做的是：

- 玩法上偏向 AI Dungeon / Fallen London。
- 展示上借用少量视觉小说元素。
- 美术资源不是 MVP 前提。
- 酒馆可以作为 AI 编排和上下文运行时。

更准确的描述：

> 一个带轻量视觉小说包装的 AI 互动文字冒险。

## 两天 MVP 目标

必须完成：

- 一个可运行的前端主界面。
- 当前事件展示。
- 剧情文本展示。
- 2 到 4 个固定选项。
- 一个自由输入框。
- 点击选项或提交输入后推进一轮剧情。
- mock AI fallback。
- 状态面板。
- 历史记录。
- 一条完整可演示流程。

暂时不要求：

- 完整立绘。
- CG。
- BGM。
- 复杂动画。
- 多路线完整结局。
- 后端管理系统。
- 完整角色卡 PNG 写回工具。

## 技术栈

前端：

- Vite
- React
- TypeScript
- CSS Modules 或普通 CSS
- localStorage 保存临时进度

AI 接入：

- 第一阶段：mock `generateScene(input)`。
- 第二阶段：替换为真实模型 API 或酒馆输出。
- 第三阶段：用酒馆角色卡、世界书、预设和正则稳定生产内容。

## 运行模式

项目可以支持两种模式。

### 模式 A：独立网页模式

用于黑客松演示和本地开发。

```text
玩家操作
  -> 前端状态
  -> generateScene(input)
  -> mock 或 API 返回 JSON
  -> 前端更新 UI
```

优点：

- 最容易演示。
- 不依赖酒馆环境。
- 可以快速调前端和交互。

### 模式 B：酒馆联动模式

用于复用酒馆能力。

```text
玩家操作
  -> 前端生成行动文本
  -> 酒馆聊天上下文
  -> 角色卡 + 世界书 + 预设约束模型
  -> 模型输出 <event_json>
  -> 正则捕获
  -> 前端渲染
```

优点：

- 复用酒馆的上下文、角色卡、世界书、预设和正则。
- 不需要从零实现完整 AI 编排层。

## 核心数据结构

前端维护一个 `StoryState`：

```ts
type StoryState = {
  scene: string;
  time: string;
  anomalyLevel: number;
  playerStress: number;
  clues: string[];
  flags: string[];
  members: MemberState[];
  history: StoryTurn[];
};

type MemberState = {
  name: string;
  status: string;
  trust?: number;
};
```

每一轮 AI 返回一个 `StoryTurn`：

```ts
type StoryTurn = {
  eventTitle: string;
  scene: string;
  time: string;
  mood: string;
  narration: string;
  dialogue: DialogueLine[];
  stateChanges: StateChanges;
  choices: string[];
};

type DialogueLine = {
  speaker: string;
  mood: string;
  text: string;
};

type StateChanges = {
  anomalyLevelDelta: number;
  playerStressDelta: number;
  addClues: string[];
  addFlags: string[];
  memberUpdates: Partial<MemberState>[];
};
```

## 一轮交互流程

```text
1. 用户看到当前事件。
2. 用户点击固定选项，或在自由输入框写行动。
3. 前端组装 input：
   - 当前 StoryState
   - 最近几轮 history
   - 玩家行动 playerAction
4. 调用 generateScene(input)。
5. 返回 StoryTurn。
6. 校验 StoryTurn。
7. 应用 stateChanges。
8. 把新事件加入 history。
9. UI 更新。
```

## AI 输出协议

普通 API 模式可以直接返回 JSON。

酒馆模式建议包一层标签，方便正则捕获：

```text
<event_json>
{
  "eventTitle": "旧电脑的异常启动",
  "scene": "社团活动室",
  "time": "放学后",
  "mood": "日常中混入不安",
  "narration": "你走到角落的旧电脑前，屏幕在你触碰鼠标之前自己亮了起来。",
  "dialogue": [
    {
      "speaker": "团长",
      "mood": "兴奋",
      "text": "看吧！我就说今天一定会发生什么！"
    }
  ],
  "stateChanges": {
    "anomalyLevelDelta": 8,
    "playerStressDelta": 2,
    "addClues": ["旧电脑无输入启动"],
    "addFlags": ["computer_awake"],
    "memberUpdates": [
      { "name": "团长", "status": "兴奋" }
    ]
  },
  "choices": [
    "询问团长是不是提前安排了机关",
    "让文静少女检查电脑日志",
    "假装没看到，观察其他人的反应"
  ]
}
</event_json>
```

## 前端模块

```text
src/
  App.tsx
  main.tsx
  styles/
    global.css
  components/
    StoryLayout.tsx
    EventCard.tsx
    DialogueBlock.tsx
    ChoicePanel.tsx
    FreeActionInput.tsx
    StatusPanel.tsx
    HistoryLog.tsx
  lib/
    generateScene.ts
    applyStateChanges.ts
    validateTurn.ts
    storage.ts
  data/
    mockScenes.ts
    initialState.ts
```

## 校验与 fallback

AI 输出必须校验：

- 是否能解析成 JSON。
- 是否包含 `eventTitle`、`narration`、`choices`。
- `choices` 是否是 2 到 4 个字符串。
- 数值变化是否在合理范围。
- 是否缺少关键状态字段。

校验失败时：

- 使用 fallback 事件。
- 保留当前状态。
- 给用户显示“叙事暂时停顿”的剧情，而不是报错。

## 开发优先级

第一优先级：

- 交互闭环。
- 状态更新。
- mock 剧情。
- 页面可演示。

第二优先级：

- 接真实 AI。
- JSON 校验。
- fallback。
- 历史记录。

第三优先级：

- 酒馆正则 demo。
- 世界书草案。
- 角色卡草案。
- 视觉细节打磨。

## 成功标准

黑客松第一版成功的标准：

- 用户能连续玩 3 到 5 轮。
- 固定选项和自由输入都能推进剧情。
- 状态会变化。
- 页面不像普通聊天框。
- 即使 AI 输出失败，也不会崩。
- 能清楚解释后续如何接酒馆角色卡、世界书和正则。
