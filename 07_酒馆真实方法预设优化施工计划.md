# 07. 酒馆真实方法预设优化施工计划

## 目标

把当前项目从：

```text
直接把 SillyTavern preset prompt_order 编译成 messages，
然后强行追加 <event_json> 输出契约。
```

优化成更接近酒馆真实运行方法的流程：

```text
SillyTavern preset 先按酒馆方式完成宏展开、变量替换、prompt_order 编译、regex 前处理；
LLM 先按 preset 生成自然正文；
项目再把自然正文适配成 StoryTurn / event_json，用于游戏状态更新和 UI 展示。
```

这份文档不是推翻 `06_酒馆预设80相似度适配施工计划.md`，而是在 06 的工程基础上升级。

```text
06 = 证明 preset 可以接入。
07 = 让这个具体 preset 的能力真正发挥出来。
```

## 当前结论

对 `夏瑾 双鱼座 Beta 0.40.json` 的默认开关分析结果：

```text
prompts: 140
prompt_order: 2
regex_scripts: 9
默认 OpenAI PromptManager 全局 order: character_id = 100001
100001 启用条目: 32 / 57
采样: temperature=1, top_p=1, top_k=0, openai_max_tokens=32000
```

它不是普通文风 preset，而是一套依赖酒馆运行时的脚本化写作流程。

默认启用条目里包含：

```text
{{setvar::...}} 17 个
{{getvar::...}} 3 个
{{trim}} 5 个
{{// ...}} 注释宏 2 个
{{user}} 7 个
DeepSeek / ChatML 风格伪标签 36 个
thinking 标签 4 个
<正文> / </正文> 标签 3 个
```

因此，当前项目只做 `prompt_order + marker + 最小 regex` 还不够。缺少的关键运行时是：

- 宏系统：`setvar` / `getvar` / `trim` / 注释 / `user`。
- 真实输出形态：preset 要自然正文和 `<正文>...</正文>`，不是直接 JSON。
- regex placement：输入、输出、世界书内容、历史内容的不同位置处理。
- PromptManager 默认全局 order：必须以 `character_id=100001` 为第一选择。
- 历史形态：酒馆历史是自然聊天/正文，不是 `<event_json>` 块。

## 非目标

黑客松阶段不完整复刻 SillyTavern。

明确不做：

- 不迁移完整 SillyTavern 前端。
- 不实现完整 Prompt Manager UI。
- 不实现完整 World Info 递归、budget、decorators、depth、outlet。
- 不执行 preset 内的任意代码。
- 不实现任意宏语言，只实现当前 preset 必需的安全宏子集。
- 不支持 PNG metadata / CharX / BYAF 全量导入。
- 不重写存档、结局、世界状态、角色选择 UI。
- 不删除 legacy 模式。

## 强制边界

### 必须保留

这些仍是项目核心价值：

- `StoryView` 的游戏主循环。
- `WorldState` / `applyTurn` / `timeAdvance`。
- 存档和读档。
- 角色卡、世界状态、结局系统。
- legacy prompt 作为安全 fallback。
- 当前 provider 层：OpenAI-compatible / Anthropic / mock。

### 必须降权或旁路

ST 真实方法模式下，这些不能继续主导：

- 旧项目的长篇输出协议。
- 旧项目 Author's Note 文风规则。
- 角色卡 `system_prompt` / `post_history_instructions` 中和输出格式相关的内容。
- `event_json` 对正文写作方式的直接强压。
- 把 assistant 历史保存为 JSON 后再喂给下一轮。
- 把 `{{user}}` 错误替换成角色名。

## 总体架构

新增一种更接近酒馆的运行模式：

```text
legacy
  -> 旧 promptRouter
  -> runLLM()
  -> parseEventJson()

sillytavern-preset-json
  -> 06 当前路线
  -> ST preset compiler
  -> 追加最小 event_json 契约
  -> runLLM()
  -> parseEventJson()

sillytavern-preset-natural
  -> ST macro engine
  -> ST regex input processor
  -> ST preset compiler
  -> runLLMText() 生成自然正文
  -> ST regex output processor
  -> storyTurnAdapter 自然正文转 StoryTurn
  -> applyTurn()
```

推荐先实现 `sillytavern-preset-natural`，但保留当前 `sillytavern-preset-json` 作为稳定兜底。

## 模块设计

### 1. `macroEngine.ts`

新增：

```text
src/lib/sillytavern/macroEngine.ts
```

职责：

实现安全的酒馆宏子集。

第一版支持：

```text
{{setvar::key::value}}  -> 记录变量，不输出文本
{{getvar::key}}         -> 替换为变量值
{{trim}}                -> 删除，并清理相邻多余空白
{{// ...}}              -> 删除注释宏
{{user}}                -> 替换为玩家/读者身份名
{{char}}                -> 替换为角色卡名
{{input}}               -> 替换为最新输入
<USER>                  -> 替换为玩家/读者身份名
<BOT>                   -> 替换为角色卡名
```

第一版不支持：

```text
嵌套宏完整语义
条件宏
数学宏
随机宏
外部脚本
扩展插件宏
```

接口建议：

```ts
export type SillyTavernMacroState = {
  variables: Record<string, string>;
  warnings: string[];
};

export function createMacroState(seed?: Record<string, string>): SillyTavernMacroState;

export function applySillyTavernMacros(
  text: string,
  context: MarkerResolverContext,
  state: SillyTavernMacroState,
): string;
```

对该 preset 的验收：

```text
{{setvar::wordsCloud::不少于1000}} 不出现在最终 prompt。
{{getvar::wordsCloud}} 被替换成 不少于1000。
{{getvar::JailbreakPrompt}} 被替换成变量内容。
{{trim}} 不出现在最终 prompt。
{{// ...}} 注释不出现在最终 prompt。
{{user}} 不再等于角色名，而是玩家身份名。
```

### 2. `promptOrderSelector.ts`

新增：

```text
src/lib/sillytavern/promptOrderSelector.ts
```

职责：

按酒馆 OpenAI PromptManager 的真实默认策略选择 prompt_order。

该 preset 应优先选择：

```text
character_id = 100001
```

因为 SillyTavern OpenAI PromptManager 配置中：

```text
promptOrder.strategy = global
dummyId = 100001
```

选择策略：

```text
1. 如果 options.preferredCharacterId 存在，优先匹配。
2. OpenAI / Chat Completion preset 默认优先 100001。
3. 找不到 100001 时，再退回启用条目最多的 order。
4. 最后退回 prompts 原始顺序。
```

验收：

```text
夏瑾 preset 默认选中 100001。
trace 显示 promptOrderSource: openai-global-dummy-id。
enabledPromptCount = 32。
```

### 3. `regexEngine.ts` 升级

已有：

```text
src/lib/sillytavern/regexEngine.ts
```

需要扩展为更接近酒馆 placement：

```text
USER_INPUT = 1
AI_OUTPUT = 2
SLASH_COMMAND = 3  暂不接
WORLD_INFO = 5
REASONING = 6      暂不接
```

第一阶段实现：

- USER_INPUT：生成前处理最新输入。
- AI_OUTPUT：自然正文生成后清洗。
- WORLD_INFO：World Info 内容插入前清洗。

需要注意：

该 preset 的 regex 多数 `promptOnly: true`，在酒馆里仍会影响 prompt / 输出文本流。当前项目不要简单理解为“只用于 prompt，不用于显示”，应按 placement 判断。

验收：

```text
最新输入被包裹为 <最新互动>...</最新互动>。
自然正文中的 <正文> / </正文> 能按 regex 清理。
破折号处理 regex 生效。
八股抹除 regex 至少在 narration 文本上生效。
trace 显示 regexHits。
```

### 4. `naturalHistory.ts`

新增：

```text
src/lib/prompting/naturalHistory.ts
```

职责：

把项目历史转成酒馆更容易理解的自然文本历史。

输入：

```text
ChatMessage[]，其中 assistant 可能有 parsed StoryTurn。
```

输出：

```text
用户输入自然文本
上一轮正文自然文本
对白自然文本
```

规则：

- ST natural 模式不把 `<event_json>` 放回 `chatHistory`。
- assistant 历史优先用 `parsed.narration + dialogue`。
- user 历史保留用户真实输入，但可以应用 USER_INPUT regex。
- 历史标签尽量贴近该 preset 的 `<互动历史>` 结构，而不是 OpenAI role dump。

验收：

```text
compiled chatHistory 中不出现 <event_json>。
compiled chatHistory 中不出现 JSON 字段名 eventTitle / stateChanges / timeAdvance。
上一轮正文能自然出现在 <互动历史> 内。
```

### 5. `stNaturalPromptEngine.ts`

新增：

```text
src/lib/prompting/stNaturalPromptEngine.ts
```

职责：

组合一轮真正接近酒馆的自然正文生成 prompt。

输入仍来自：

```ts
buildPromptForTurn(input)
```

但输出分为：

```ts
{
  messages: ChatMessage[];
  sampling: SamplingParams;
  trace: PromptBuildTrace;
  outputMode: "natural";
}
```

关键区别：

- 不追加 `MINIMAL_EVENT_JSON_CONTRACT`。
- 不要求模型输出 `event_json`。
- 保留 preset 的 `<正文>...</正文>` 语义。
- 最后只追加极短的“开始创作”触发，不写 JSON 格式要求。

可追加的最小尾部提示：

```text
请严格依据上文 preset、资料、互动历史和最新互动开始创作正文。
```

不要写：

```text
必须输出 event_json
必须包含 stateChanges
必须给 choices
```

验收：

```text
最终 prompt 中不出现 event_json。
最终 prompt 中不出现旧 outputProtocol。
最终 prompt 中 setvar/getvar/trim 已处理。
最终 prompt 中包含 <最新互动>。
最终 prompt 中包含 <相关资料> / <互动历史> / <核心指导>。
```

### 6. `storyTurnAdapter.ts`

新增：

```text
src/lib/prompting/storyTurnAdapter.ts
```

职责：

把自然正文转成项目需要的 `StoryTurn`。

第一版不要再让主模型同时承担“按 preset 写作”和“填游戏 JSON”两个任务。

推荐两种实现，按黑客松时间选择：

#### A. 规则适配，最快

规则：

```text
eventTitle: 从正文前 20-30 字生成短标题，或用“新的片段”
narration: 清理后的完整正文
dialogue: 可选，从中文引号里粗略提取，不强求
stateChanges: {}
pace: "scene"
timeAdvance: {}
requiresChoice: true
choices: 固定 3 个通用动作
```

优点：

- 不多一次 LLM 调用。
- 快、稳、便于演示。
- 最少 bug。

缺点：

- 状态更新弱。
- choices 比较通用。

#### B. 二次轻量 LLM 结构化，效果更好

流程：

```text
第一通：ST preset 生成自然正文。
第二通：极短 adapter prompt，把正文转成 event_json。
```

第二通 prompt 必须短，并明确：

```text
不要改写正文。
不要改变文风。
只提取状态、标题、选择。
narration 必须原样保留或仅做极小清理。
```

优点：

- choices / stateChanges 更好。
- 游戏系统更完整。

缺点：

- 慢。
- 多花 token。
- 失败点增加。

黑客松建议：

```text
先做 A。
如果演示需要更强交互，再做 B。
```

验收：

```text
自然正文能稳定显示到 UI。
即使正文没有 JSON，也不会触发“LLM 原始输出预览”式 fallback。
删除 / 重新生成 / 修改句子仍可用。
```

### 7. `PromptBuildTrace` 升级

修改：

```text
src/lib/prompting/types.ts
src/components/StatusPanel.tsx
```

新增 trace 字段：

```ts
outputMode?: "event-json" | "natural";
macroHits?: string[];
macroVariables?: string[];
unresolvedMacros?: string[];
promptOrderSource?: string;
regexHits?: string[];
naturalTextLength?: number;
adapterMode?: "rule" | "llm";
```

验收：

右侧调试面板至少显示：

```text
Mode: sillytavern-preset-natural
Output: natural -> rule adapter
ST order: 100001
Enabled prompts: 32
Macro vars: wordsCloud, JailbreakPrompt, cotBegin
Regex hits: 【云瑾】包裹最新指示 / ...
Unresolved macros: 0
```

### 8. Settings UI

修改：

```text
src/components/SettingsModal.tsx
```

新增模式：

```text
Legacy
ST Preset JSON Contract
ST Preset Natural Adapter
```

推荐默认：

```text
导入夏瑾 preset 后，默认切到 ST Preset Natural Adapter。
```

可选开关：

```text
玩家名 / {{user}} 替换值：默认“User”
输出适配方式：规则适配 / LLM 适配
Prompt order：自动 / 100001 / 100000
```

不要在 UI 里做完整 Prompt Manager。

## 实施步骤

## 第 0 步：建立基线

操作：

```bash
npm run typecheck
npm run build
```

记录当前可用性：

```text
legacy 可用
sillytavern-preset-json 可用
导入 preset 后右侧 trace 能看到 100001 / 32
```

回滚边界：

```text
不改主流程，只记录状态。
```

## 第 1 步：新增 prompt 诊断脚本

目的：

先能看清楚最终 prompt，而不是靠体感猜。

新增：

```text
scripts/inspect-st-preset.mjs
```

功能：

```text
读取夏瑾 preset
列出默认 order
列出 enabled prompts
统计宏
统计 unresolved macros
导出 compiled prompt snapshot
```

验收：

```bash
node scripts/inspect-st-preset.mjs "C:\Users\Marigold\Desktop\MouseWithoutBorders\夏瑾 双鱼座 Beta 0.40.json"
```

能输出：

```text
selected order: 100001
enabled: 32
setvar: 17
getvar: 3
trim: 5
```

回滚：

```text
删除 scripts/inspect-st-preset.mjs。
```

## 第 2 步：实现 macro engine

新增：

```text
src/lib/sillytavern/macroEngine.ts
```

修改：

```text
src/lib/sillytavern/markerResolver.ts
src/lib/sillytavern/presetCompiler.ts
```

做法：

1. 编译 preset 时创建一个 macro state。
2. 按 prompt_order 从前往后处理普通 prompt 内容。
3. 对每个 prompt 先处理 `setvar`，再处理 `getvar`。
4. marker 输出也跑一次基础宏替换，但不允许 marker 内容设置变量。
5. 收集 trace。

验收：

```text
最终 compiled messages 中不出现 {{setvar::
最终 compiled messages 中不出现 {{getvar::
最终 compiled messages 中不出现 {{trim}}
最终 compiled messages 中不出现 {{//
字数准则中出现“不少于1000字”
系统提示中出现 JailbreakPrompt 的实际内容
```

回滚：

```text
revert macroEngine.ts 和 presetCompiler.ts 的接线即可。
```

## 第 3 步：修正 prompt_order 选择

新增：

```text
src/lib/sillytavern/promptOrderSelector.ts
```

修改：

```text
src/lib/sillytavern/presetCompiler.ts
```

验收：

```text
夏瑾 preset 永远默认选择 100001。
trace.promptOrderSource = openai-global-dummy-id。
用户手动选择 100000 时可以切到简版。
```

回滚：

```text
恢复原“启用最多”策略。
```

## 第 4 步：补齐自然历史

新增：

```text
src/lib/prompting/naturalHistory.ts
```

修改：

```text
src/lib/sillytavern/markerResolver.ts
src/components/StoryView.tsx
```

验收：

```text
ST natural 模式 chatHistory 不含 event_json。
第二轮生成时，history 中能看到上一轮自然正文。
修改句子后，history 中替换的是自然正文。
legacy 模式不受影响。
```

回滚：

```text
只让 ST natural 模式禁用，保留 ST JSON 模式。
```

## 第 5 步：ST natural prompt engine

新增：

```text
src/lib/prompting/stNaturalPromptEngine.ts
```

修改：

```text
src/lib/prompting/index.ts
src/lib/prompting/types.ts
```

新增模式：

```ts
type PromptMode =
  | "legacy"
  | "sillytavern-preset"
  | "sillytavern-preset-natural";
```

验收：

```text
natural 模式最终 messages 不包含 <event_json> 输出契约。
natural 模式最终 messages 包含该 preset 的 <输出模板>。
natural 模式最终 messages 包含 <正文> 要求。
```

回滚：

```text
SettingsModal 默认切回 sillytavern-preset JSON 模式。
```

## 第 6 步：规则版 storyTurnAdapter

新增：

```text
src/lib/prompting/storyTurnAdapter.ts
```

修改：

```text
src/components/StoryView.tsx
```

流程：

```text
if mode === sillytavern-preset-natural:
  rawNatural = runLLMText(messages, sampling)
  cleanedNatural = apply AI_OUTPUT regex
  turn = adaptNaturalTextToStoryTurn(cleanedNatural)
else:
  runLLM()
```

验收：

```text
模型输出自然正文时，UI 能显示正文。
不会因为没有 event_json 进入 fallback。
choices 至少有 3 个可点选项。
重新生成仍可复用同一 checkpoint。
```

回滚：

```text
关闭 natural 模式，不影响旧 JSON 模式。
```

## 第 7 步：可选 LLM adapter

新增：

```text
src/lib/prompting/llmStoryTurnAdapter.ts
```

前提：

规则版已经跑通。

做法：

第二次调用 LLM：

```text
输入：自然正文 + 当前 state 摘要
输出：event_json
要求：narration 原样保留，不重写正文
```

验收：

```text
stateChanges 比规则版更丰富。
choices 更贴合正文。
narration 与自然正文差异很小。
失败时自动回退规则版。
```

回滚：

```text
adapterMode 切回 rule。
```

## 第 8 步：regex WORLD_INFO placement

修改：

```text
src/lib/prompting/index.ts
src/lib/sillytavern/regexEngine.ts
```

做法：

World Info entry content 插入前跑：

```text
placement = WORLD_INFO
```

验收：

```text
trace 显示 worldInfoRegexHits。
worldInfoBefore/worldInfoAfter 中不出现应被 regex 清理的格式残留。
```

回滚：

```text
禁用 WORLD_INFO placement，不影响输入/输出 regex。
```

## 第 9 步：真实模型最小验收

不要做大规模测试，只做黑客松必要验证。

### 验证 1：编译快照

不调用 LLM。

通过条件：

```text
order = 100001
enabled = 32
unresolvedMacros = 0 或只有明确暂不支持的无害宏
包含 <相关资料>
包含 <互动历史>
包含 <核心指导>
包含 <最新互动>
不包含 event_json
```

### 验证 2：单轮自然正文

输入：

```text
（故事开始。请自然展开本轮事件。）
```

通过条件：

```text
返回自然正文。
正文风格接近 preset：轻小说、长段落、对白分离、第三人称。
UI 不触发 fallback。
trace 显示 macro / regex / natural adapter。
```

### 验证 3：三轮连续

流程：

```text
开场
点击一个 choice
继续推进
```

通过条件：

```text
第二轮 prompt 历史中有上一轮自然正文。
没有 event_json 历史污染。
至少 2/3 轮 UI 正常推进。
```

### 验证 4：回滚

切回：

```text
legacy
```

通过条件：

```text
旧模式仍能开场。
mock 仍能跑。
```

## 与当前项目的主要冲突

### 冲突 1：第三人称 vs 项目第一人称 UI

该 preset 默认启用：

```text
剧情全程务必严格采用第三人称创作。
```

当前项目 UI 仍有第一人称 POV 文案。

处理方式：

```text
ST natural 模式下，不再强制第一人称。
UI 文案后续可弱化为“当前主角 / 角色视角”，不要写死第一人称。
```

### 冲突 2：正文模板 vs event_json

该 preset 默认启用：

```text
<正文>
剧情正文
</正文>
```

当前项目要求：

```text
<event_json>...</event_json>
```

处理方式：

```text
生成阶段尊重 <正文>。
游戏阶段用 adapter 转 StoryTurn。
```

### 冲突 3：宏系统缺失

该 preset 的默认启用 prompt 很多内容依赖：

```text
setvar / getvar / trim
```

处理方式：

```text
先实现安全宏子集。
不要让原始宏泄漏给模型。
```

### 冲突 4：伪 ChatML 与 provider messages

该 preset 含：

```text
<｜User｜>
<｜Assistant｜>
<｜end▁of▁sentence｜>
```

处理方式：

```text
第一阶段原样保留，因为这是 preset 设计的一部分。
如果目标模型不是 DeepSeek / 兼容该格式，可在 UI 中加“清理伪 ChatML”开关。
不要默认删除，否则会改变 preset 原意。
```

### 冲突 5：NSFW 强化默认开启

该 preset 默认启用 NSFW 强化。

处理方式：

```text
忠实适配时保留默认开关。
公开演示如果不需要，可在 Prompt Manager-lite 中手动关闭该条。
不要在编译器里偷偷删除。
```

## 最小落地路线

如果只有很短时间，按这个顺序做：

```text
1. macroEngine
2. promptOrderSelector 固定 100001
3. ST natural mode，不追加 event_json
4. 规则版 storyTurnAdapter
5. trace 显示 unresolved macros / regex / adapter
```

这五步完成后，preset 的体感会比继续修 JSON 模式明显好。

暂时不要先做：

```text
完整 World Info
完整 Prompt Manager
完整 regex allow-list
PNG 角色卡
LLM adapter
```

## 失败回退策略

### 如果 natural 模式经常乱输出

回退：

```text
保留 ST macro engine
回到 sillytavern-preset JSON 模式
```

也就是说，即使 natural adapter 失败，宏展开也能改善当前 JSON 模式。

### 如果 adapter 状态更新太弱

先接受：

```text
stateChanges = {}
固定 choices
```

黑客松演示优先展示文风和互动连续性。

### 如果 regex 误伤正文

回退：

```text
只保留 USER_INPUT regex
关闭 AI_OUTPUT regex
```

### 如果 prompt 过长

优先关闭：

```text
最大推理强度
伪对话
NSFW强化
更强穿甲
```

不要先裁剪角色卡和历史。

## 文件变更清单

第一阶段新增：

```text
src/lib/sillytavern/macroEngine.ts
src/lib/sillytavern/promptOrderSelector.ts
src/lib/prompting/naturalHistory.ts
src/lib/prompting/stNaturalPromptEngine.ts
src/lib/prompting/storyTurnAdapter.ts
scripts/inspect-st-preset.mjs
```

第一阶段修改：

```text
src/lib/sillytavern/presetCompiler.ts
src/lib/sillytavern/markerResolver.ts
src/lib/sillytavern/regexEngine.ts
src/lib/prompting/index.ts
src/lib/prompting/types.ts
src/components/StoryView.tsx
src/components/SettingsModal.tsx
src/components/StatusPanel.tsx
```

不应修改：

```text
src/lib/llmProviders.ts
src/lib/worldState.ts
src/lib/timeAdvance.ts
src/types/turn.ts
src/data/presets/index.ts
src/data/lorebooks/hsuzumiya_lore.ts
```

## 最终验收标准

工程验收：

```text
npm run typecheck 通过
npm run build 通过
legacy 可用
sillytavern-preset-json 可用
sillytavern-preset-natural 可用
```

Prompt 验收：

```text
默认 order = 100001
enabled prompts = 32
setvar/getvar/trim/comment 宏不泄漏
{{user}} 替换正确
<最新互动> 出现
event_json 不出现在 natural 生成 prompt
```

效果验收：

```text
输出明显是自然正文，而不是 JSON 味文本。
轻小说 / 长段落 / 对白分离 / 第三人称开关能体现。
第二轮能延续上一轮自然正文。
choices 可点击，状态系统不崩。
```

安全验收：

```text
不执行 preset 中任何代码。
不读取本地绝对路径。
不破坏 legacy。
adapter 失败时有规则兜底。
```

## 压缩上下文后的继续规则

之后如果发生上下文压缩或恢复，继续施工前必须重新读取：

```text
D:\Aheikesong\text-based_adventure_game\06_酒馆预设80相似度适配施工计划.md
D:\Aheikesong\text-based_adventure_game\07_酒馆真实方法预设优化施工计划.md
D:\Aheikesong\research\sillytavern_preset_world_character_regex_research.md
```

读取顺序：

```text
先读 06，确认原边界。
再读 07，确认新路线。
最后读 research，确认酒馆真实行为来源。
```

## 一句话施工原则

```text
不要继续逼酒馆 preset 直接吐 event_json；
先让它按酒馆方法生成自然正文，
再用项目自己的 adapter 把正文变成游戏状态。
```
