# 10. 系统调用管理与上下文编排优化方案

## 先读规则

如果后续发生上下文压缩，继续施工前必须重新读取：

- `09_角色卡RAG与剧情触发系统设计文档.md`
- `10_系统调用管理与上下文编排优化方案.md`

本文不是替代 09 文档，而是把 09 里的 RAG、Plot Governor、平淡度检测、进度钟，和当前项目里的世界书、角色卡、时间线、提示词构建方式，合并成一个更明确的“系统调用管理器”施工方案。

一句话目标：

```text
由系统决定本轮该给模型什么资料、该发生什么剧情、该用多少上下文、该如何解析和校验；LLM 只负责把已调度好的内容写好。
```

核心口号继续沿用 09 文档：

```text
RAG 管“能写什么”，Plot Governor 管“现在该发生什么”，平淡度检测管“别一直没事发生”，LLM 只负责写得好看。
```

## 为什么需要系统调用管理器

现在项目已经有很多好东西，但它们还没有被一个统一入口稳定编排：

| 资产 / 模块 | 当前价值 | 当前问题 | 管理器应该怎么接管 |
|---|---|---|---|
| `hsuzumiya_lore.ts` | 已有凉宫世界事实、地点、NPC、事件链资料 | 约 40 条，其中 14 条 constant，核心 NPC 和地点常驻过多 | 统一转成 RAG 条目，真正 constant 只保留硬规则 |
| `npcReferenceCards` | 10 张原作 NPC 厚卡，约 12 万字 | 现在主要是参考资料，没有进入运行时按需检索 | 拆成 NPC RAG 条目，按 sceneCast / storylet / 关键词命中 |
| 玩家角色卡 | 能承载玩家原创身份 | writer-adapter 当前会注入较完整卡内容，容易占预算 | 保留厚卡，运行时只注入 POV 核心 + 当前相关片段 |
| `canonTimeline.ts` | 原作时间线很完整，能强制主线 | 目前更像时间窗口文本，不直接参与剧情调度评分 | 进入 Plot Governor，作为最高优先级 canon focus |
| `eventChains.ts` | 有 8 条长期事件链 | 只有链元数据，没有具体 storylet 和阶段目标 | 拆成 arc stage / storylet / clock gate |
| `selectLoreEntries()` | 已有 constant、关键词、identity、chain 门控 | 缺 sceneCast、location、date、flags、relation、storylet 强制激活 | 外层升级为 deterministic RAG selector |
| `writerAdapterPromptEngine.ts` | 已支持自然正文 + 结构适配 | 资料选择和剧情意图仍太依赖 prompt 本身 | 改为接收 OrchestratedTurnPlan |
| `PromptBuildTrace` / `StatusPanel` | 已能展示 activeLoreEntries 等调试信息 | 还看不到 RAG 原因、storylet、平淡度、clock、预算 | 扩展 trace，变成可验证调度面板 |
| `llmProviders.ts` | DeepSeek 官方端点已发送 `thinking: { type: "disabled" }` | 调用意图、采样、预算还散在 prompt engine 中 | 由 LLM Call Manager 根据任务类型统一决定 |

目前最核心的问题不是“模型读得不够多”，而是“模型每轮读到的东西不够准”。DeepSeek 支持长上下文，但长上下文不会自动带来好文笔，也不会自动带来剧情推进。长上下文如果塞进互相冲突的规则、完整人物卡、重复禁止句式，反而会稀释注意力。

所以要做一个中间层：

```text
前端 / 用户选择
  -> 系统调用管理器
      -> 内容资产检索
      -> 剧情节点调度
      -> 上下文预算
      -> prompt 计划
      -> LLM 调用
      -> 输出解析与状态校验
  -> 前端展示 / 存档
```

## 总体边界

### 必须做到

- 保持前端点击式阅读、存档、状态展示的现有体验。
- 保持角色卡、世界书、时间线、事件链这些内容资产可继续扩写。
- 允许角色卡很厚，但禁止每轮无脑整张注入。
- 允许世界书很大，但必须按 scene / entity / date / flag / relation / storylet 选择。
- 允许 prompt 有文风层，但文风层不能夹带剧情事实。
- 事件发生时机由代码侧 Plot Governor 决定，不让 LLM 自己随机决定主线是否发生。
- LLM 输出的 stateChanges 必须经过代码校验，不能直接全信。
- 每个阶段都要能在 trace 或单元测试里验证。

### 暂时不做

- 不直接嵌入完整 SillyTavern 运行时。
- 不在黑客松阶段引入向量数据库、服务器端 embedding 管线或复杂 GraphRAG。
- 不把所有历史无脑塞到 1M 上下文。
- 不额外调用 LLM 做平淡度检测，先用规则版。
- 不把 NPC 的 `system_prompt`、`post_history_instructions` 直接塞给正文模型，避免抢 POV 和输出协议冲突。
- 不用大量“禁词清单”解决文风问题；只保留少量高信号硬规则，主要靠文风画像、参考片段和剧情调度解决。

## 目标架构

每轮生成都走同一个编排流水线：

```text
TurnContextSnapshot
  -> ContentAssetIndex
  -> PlotDecision
  -> RagSelection
  -> ContextBudget
  -> PromptPlan
  -> LLMResult
  -> ParsedTurn + Metadata
  -> ValidatedStateUpdate
  -> OrchestrationTrace
```

建议新增一个总入口：

```ts
type OrchestratedTurnPlan = {
  snapshot: TurnContextSnapshot;
  plot: PlotDecision;
  rag: RagSelectionResult;
  prompt: PromptBuildOutput;
  diagnostics: OrchestrationTrace;
};
```

正文生成不再由 `writerAdapterPromptEngine` 自己决定“注入什么资料”。它只负责把 `PromptPlan` 渲染成 messages。

## 每轮流水线详解

### 1. TurnContextSnapshot

职责：收集本轮所有可观测状态，不做判断。

输入来源：

- 当前 `WorldState`
- 当前玩家卡 id 和玩家卡数据
- 最近 history / parsed beats / rolling summary
- 当前 userInput
- 当前 sceneCast
- 当前 timeline window
- 当前 startingPoint
- 当前 UI 模式、输出模式、LLM 设置

建议类型：

```ts
type TurnContextSnapshot = {
  state: WorldState;
  playerCardId: string;
  userInput: string;
  recentHistory: ChatMessage[];
  summary?: string | null;
  sceneCastEntityIds: string[];
  sceneCastRaw?: string;
  locationTags: string[];
  timelineWindow: TimelineWindow;
  currentCanonFocus?: CanonEvent;
};
```

验证边界：

- snapshot 只读，不改状态。
- 同一个输入必然生成同一个 snapshot。
- sceneCast 必须被标准化成 entityIds，不能只作为自然语言放进 Author's Note。

### 2. ContentAssetIndex

职责：把所有内容资产统一变成可检索条目。

资产来源：

| 来源 | 处理方式 |
|---|---|
| `hsuzumiya_lore.ts` | 转成 `RagEntry`，保留 keys / position / identity_gate / chain_id |
| 玩家角色卡 | 拆成 POV、公开资料、私密资料、关系、语气、背景、当前记忆 |
| NPC 角色卡 | 拆成 profile、voice、behavior、relationship、example，不直接注入系统提示 |
| `canonTimeline.ts` | 每个 canon event 变成 dateWindow + participants + location 的资料条目 |
| `eventChains.ts` | 每条 chain 变成 arc 资料，后续拆成 stage / storylet |
| Runtime State | 每轮临时生成 runtime 条目，不写回资产库 |
| 参考小说 / 文风画像 | 只进入 style layer，不进入事实 RAG |

推荐新增文件：

```text
src/types/rag.ts
src/lib/rag/cardToRagEntries.ts
src/lib/rag/loreToRagEntries.ts
src/lib/rag/canonToRagEntries.ts
src/lib/rag/contentAssetIndex.ts
```

角色卡拆分原则：

```text
保留厚卡作者体验，运行时只选择片段。

NPC system_prompt：不直接注入，避免抢 POV。
NPC post_history_instructions：不注入，避免输出协议冲突。
NPC first_mes：通常不注入。
NPC description/personality/mes_example：拆成 profile / voice / example RAG。
```

玩家卡处理：

```text
每轮必注入：
- 当前 POV 是谁
- 第一人称边界
- 玩家不能被 LLM 代替决定
- 当前身份等级和公开身份

按需注入：
- 背景经历
- 性格细节
- 秘密身份
- 与某 NPC 的关系
- 当前地点相关记忆
```

验证边界：

- 10 张 NPC 卡都能被索引，但默认不进入 prompt。
- 一张 9000 字玩家卡最终 prompt 不应完整注入 9000 字。
- NPC 卡的输出协议字段不进入正文 prompt。

### 3. Entity Resolver

职责：把中文名、别名、sceneCast 文本、canon participants 统一映射为实体 id。

示例：

```text
凉宫春日 / 春日 / 团长 -> haruhi
阿虚 / キョン -> kyon
长门 / 长门有希 -> nagato
朝比奈 / 实玖瑠 / 学姐 -> asahina
古泉 / 古泉一树 -> koizumi
```

强制激活例子：

```text
sceneCast 里出现“凉宫春日” -> force entityId=haruhi
canonFocus.participants 包含“阿虚” -> force entityId=kyon
activeStorylet.ragHints.forceEntityIds -> force those entries
```

推荐新增文件：

```text
src/lib/rag/entityResolver.ts
src/data/entities.ts
```

验证边界：

- userInput 没写“春日”，但 sceneCast 有春日时，仍命中 `char_haruhi_core`。
- canonFocus participants 有阿虚时，阿虚基础人设必进 prompt。
- 别名命中不应误伤普通词，例如“团”不能单独触发所有 SOS 条目。

### 4. Plot Governor

职责：决定本轮“该发生什么”，不写正文。

输入：

```text
snapshot
canonTimeline pending events
activeChain
storylet library
progress clocks
blandnessScore
recent player action
cooldown records
```

输出：

```ts
type PlotDecision = {
  eventKind: EventPresetKind;
  activeStorylet?: StoryletNode;
  canonFocus?: CanonEvent;
  reason: string;
  intensityTarget: number;
  allowedFreedom: string[];
  fixedFacts: string[];
  variableOutcomes: string[];
  ragHints: {
    forceEntityIds: string[];
    forceLocationTags: string[];
    forceEntryIds: string[];
  };
};
```

优先级：

```text
1. 到期 main_line canon event
2. activeChain 当前阶段
3. clock 满格后解锁的 storylet
4. 高平淡度触发 micro / relationship / rumor
5. 玩家输入强相关 storylet
6. 普通日常推进
```

关键点：

- canon 事件本体不可改，但玩家切入角度可变。
- storylet 是“钩子 + 冲突 + 可变结果”，不是完整脚本。
- 平淡度高时不是强行爆大事件，而是补一个有后果的小动作。

推荐新增文件：

```text
src/types/storylet.ts
src/data/storylets.ts
src/lib/plot/plotGovernor.ts
src/lib/plot/storyletSelector.ts
```

验证边界：

- 入学日应优先选择 `entrance_day_declaration` canon focus。
- 同一 storylet 进入 cooldown 后不能连续出现。
- 玩家主动接近某 NPC，应提高该 NPC relationship storylet 权重。

### 5. Blandness Meter

职责：检测剧情有没有推进，不评价文笔好坏。

平淡不是“安静”。安静可以很有张力。真正平淡是：

```text
没有目标，没有阻碍，没有变化，没有后果，没有下一拍。
```

规则版输入：

- 最近 3 到 5 个 parsed turn
- 是否有 relationUpdates
- 是否 addFlags / addClues
- 是否 locationShift
- 是否 new sceneCast
- choices 是否都是“继续观察 / 等等看”
- 是否连续只有环境描写和内心判断
- 是否有 unresolved thread

推荐输出：

```ts
type BlandnessReport = {
  score: number;
  reasons: string[];
  suggestedTiers: Array<"micro" | "relationship" | "rumor" | "anomaly">;
};
```

推荐新增文件：

```text
src/lib/plot/blandness.ts
```

验证边界：

- 连续 3 批无 relation / flag / clue，`blandnessScore > 60`。
- 出现新角色主动反应后，分数下降。
- 高平淡度时 Plot Governor 选择 micro / relationship，而不是继续纯场景描写。

### 6. Progress Clocks

职责：管理长期剧情推进，不让 LLM 靠感觉跳剧情。

建议第一批 clocks：

| id | max | 用途 |
|---|---:|---|
| `haruhi_attention` | 4 | 春日是否开始记住玩家 |
| `sos_involvement` | 6 | 玩家卷入 SOS 团程度 |
| `class_rumor_heat` | 4 | 班级和走廊流言热度 |
| `nagato_attention` | 6 | 长门是否开始观察玩家 |
| `kyon_trust` | 6 | 阿虚是否把玩家当作可交流对象 |
| `anomaly_pressure` | 8 | 异常苗头压力 |
| `blandness_recovery` | 3 | 避免连续强插事件的缓冲 |

推荐新增文件：

```text
src/types/clocks.ts
src/lib/plot/progressClocks.ts
```

验证边界：

- clock tick 后能在 trace 中看到原因。
- clock 不超过 max，不低于 0。
- clock 满格只解锁候选 storylet，不让 LLM 自由发明大事件。

### 7. RAG Selector

职责：根据 PlotDecision 和 snapshot 选择本轮资料。

选择顺序：

```text
1. 固定最小 POV / 硬规则
2. Runtime State
3. PlotDecision 强制条目
4. sceneCast 强制角色
5. 当前 location 强制地点
6. canonFocus / activeStorylet 相关资料
7. identity / date / flag / relation / chain gate
8. 最近历史和 userInput 关键词
9. 预算内补充高优先级条目
```

世界书 constant 收口：

```text
真正 constant：
- 玩家不可取代原作角色
- 原作硬事实最短版
- 第一人称 / 不读心 / 玩家代理权
- 最小输出协议

不该 constant：
- 阿虚完整人设
- 春日完整人设
- 长门 / 朝比奈 / 古泉完整人设
- 地点细节
- 事件细节
- 文风描述
```

推荐新增文件：

```text
src/lib/rag/ragSelector.ts
src/lib/rag/ragScoring.ts
```

验证边界：

- 当前地点是文艺部部室时，部室条目命中。
- 路人身份下，不命中超自然真相条目，除非只是用“异常感”方式提示。
- `sceneCast` 有春日但 userInput 没写春日，春日核心人设仍命中。
- 核心 NPC 不在 sceneCast / canon / storylet 中时，不应全员常驻。

### 8. Context Budgeter

职责：决定每层最多给多少上下文，防止“越塞越散”。

DeepSeek 长上下文可以利用，但不要把 1M 当垃圾桶。推荐先做可解释预算：

| 层 | 黑客松建议预算 |
|---|---:|
| 文风画像 | 500-1200 中文字 |
| 少量参考片段 | 0-1500 中文字，按场景可选 |
| 最小 POV / 硬规则 | 150-300 中文字 |
| Runtime State | 300-700 中文字 |
| 当前在场 NPC | 每人 120-300 中文字 |
| 当前地点 | 80-180 中文字 |
| activeStorylet / canonFocus | 200-500 中文字 |
| 其他 RAG 命中 | 总计 600-1500 中文字 |
| 最近历史 | 最近 6-12 个 beat |
| rolling summary | 1000-3000 中文字 |

长上下文策略：

```text
短期：最近 6-12 个 beat 原文。
中期：rolling summary 保留因果、关系、线索和未解决钩子。
长期：facts ledger / clocks / flags / relations 由结构化状态维护。
参考文风：每轮注入精炼画像；需要时再注入少量风格片段，不混进剧情事实。
```

压缩策略：

```text
当 history 超过预算：
1. 保留最近 beat 原文。
2. 把更早内容压成 rolling summary。
3. 把确定事实写进 flags / clues / relations / clocks。
4. 删除已经被结构化状态覆盖的重复自然语言。
```

推荐新增文件：

```text
src/lib/rag/contextBudgeter.ts
```

验证边界：

- 同样的资产池，在不同预算下输出条目数可预测。
- 超预算时先删低优先级可恢复资料，不删 POV 硬规则和 activeStorylet。
- trace 能显示每层实际字符数 / 估算 token 数。

### 9. Prompt Plan Builder

职责：把预算后的资料排成稳定 prompt 顺序。

推荐顺序：

```text
1. 文风层
   只管怎么写，不写剧情事实。

2. 最小 POV 层
   当前“我”是谁，不能替玩家决定，不读心，不越权。

3. 本轮 RAG 资料
   在场角色、地点、世界事实、canon / storylet 资料。

4. 近期历史
   最近 beat + rolling summary。

5. Runtime State
   日期、身份、关系、flags、clues、clocks。

6. Plot Governor 钩子
   本轮冲突、固定事实、可变结果、强度目标。

7. 最小输出协议
   前端必须解析的协议，尽量短。

8. 当前玩家行动
```

注意：

- Runtime State 和 Plot Governor 应靠近末尾，类似 SillyTavern Author's Note。
- 输出协议只放一份，避免 preset、角色卡、世界书多处重复。
- 文风层必须和剧情事实分离，避免“模仿原著风格”时把原著剧情也带进来。

推荐新增文件：

```text
src/lib/prompting/orchestratedPromptEngine.ts
src/lib/orchestration/contextOrchestrator.ts
```

验证边界：

- prompt trace 能展示每一层的内容摘要。
- 输出协议在最终 messages 中只出现一次。
- 文风层不包含 canon 事件事实。

### 10. LLM Call Manager

职责：统一决定调用参数、超时、模式和错误处理。

当前 `llmProviders.ts` 已在 DeepSeek 官方端点加入：

```ts
body.thinking = { type: "disabled" };
```

管理器后续应把调用任务分为：

| call kind | 用途 | temperature | max tokens |
|---|---|---:|---:|
| `story_prose` | 正文写作 | 较高 | 中等 |
| `story_structured` | 模型自己写并按 beat 切分 | 中等 | 中高 |
| `metadata` | 状态变化和 choices | 低 | 低 |
| `summary` | 历史压缩 | 低中 | 中 |
| `repair` | JSON 修复 | 低 | 低 |

如果继续采用“模型自己写自己切”的方向，优先做单次 `story_structured`，让模型在同一上下文里保持整体文笔，再输出 beats。只有 JSON 崩坏时才走 `repair`。

验证边界：

- DeepSeek 官方端点 payload 包含 `thinking: { type: "disabled" }`。
- `story_prose` 不使用过低 temperature。
- `metadata / repair` 不使用高 temperature。
- 402、超时、网络错误在 UI 中给出明确错误，不污染历史。

### 11. Parser / Validator / State Applier

职责：解析 LLM 输出，并校验哪些状态变化可信。

解析层：

- 清理多余前缀。
- 提取唯一 `<event_json>`。
- 验证 JSON 合法。
- 将 beats 中 narration / dialogue 分开，dialogue 只把台词放进特别 UI。
- TTS 字段只服务语音，不写进正文。

校验层：

| 字段 | 校验方式 |
|---|---|
| `identityShift` | 必须满足身份规则，不允许跳级 |
| `triggeredCanonEvents` | 只能由代码侧写入 |
| `stateChanges.relationUpdates` | 限制单次变化幅度 |
| `tickClocks` | id 必须存在，delta 有上限 |
| `storyletResult.id` | 必须等于当前 activeStorylet |
| `addFlags` | 不允许加入和 canon 冲突的 flag |
| `sceneCastAdd/Remove` | 必须能解析到 entityId 或保留为 raw pending |

推荐新增文件：

```text
src/lib/orchestration/turnValidator.ts
src/lib/orchestration/stateApplier.ts
```

验证边界：

- LLM 输出不存在的 clock id 会被拒绝并进入 warning。
- LLM 试图把玩家改成阿虚时被拒绝。
- LLM 输出两个 `<event_json>` 时只接受第一个合法块，并记录 warning。

### 12. Orchestration Trace

职责：让每一次生成都能解释“为什么是这个结果”。

建议扩展 `PromptBuildTrace`：

```ts
type OrchestrationTrace = {
  mode: PromptMode;
  presetName: string;
  messageCount: number;
  sampling?: SamplingParams;

  activeLoreEntries: string[];
  forcedRagEntries: string[];
  filteredLoreEntries: string[];
  ragStats: {
    totalCandidates: number;
    selectedCount: number;
    droppedByBudget: string[];
    budgetByLayer: Record<string, number>;
  };

  plotDecision?: {
    eventKind: string;
    storyletId?: string;
    canonEventId?: string;
    reason: string;
    intensityTarget: number;
  };

  blandness?: {
    score: number;
    reasons: string[];
  };

  clocks?: Array<{
    id: string;
    value: number;
    max: number;
    delta?: number;
    reason?: string;
  }>;

  warnings?: string[];
};
```

前端 `StatusPanel` 可以先只展示：

- active storylet
- canon focus
- blandness score
- active clocks
- forced RAG
- budget warning

验证边界：

- 每次生成后开发面板能看到“为什么春日资料被注入”。
- 高平淡度触发 storylet 时能看到 reasons。
- 被预算丢弃的条目能被列出前几个。

## 当前文件映射

| 当前文件 | 未来角色 |
|---|---|
| `src/lib/promptRouter.ts` | 保留 legacy / ST path；其中 `selectLoreEntries` 可作为旧 selector，外层新增 RAG selector |
| `src/lib/prompting/writerAdapterPromptEngine.ts` | 后续改为接收 `OrchestratedTurnPlan`，不再自己决定完整资料注入 |
| `src/lib/prompting/types.ts` | 扩展 trace 字段 |
| `src/data/lorebooks/hsuzumiya_lore.ts` | 保留为主世界书，逐步补 `entityIds/locationTags/dateWindow/flags` |
| `src/data/characters/index.ts` | 作为 NPC 资产入口进入 `ContentAssetIndex` |
| `src/data/canonTimeline.ts` | 进入 Plot Governor 和 RAG index |
| `src/data/eventChains.ts` | 作为 arc 定义，后续补 storylet stage |
| `src/types/worldState.ts` | 后续增加 `clocks/activeStorylet/locationTags/sceneCast` 等状态 |
| `src/lib/summary.ts` | 继续负责 rolling summary，但不作为唯一权威记忆 |
| `src/components/StatusPanel.tsx` | 扩展为调度 trace 可视化 |

## 分阶段施工计划

### 阶段 1：只加类型和调试字段

目标：不改变运行行为，先建立边界。

改动：

- 新增 `src/types/rag.ts`
- 新增 `src/types/storylet.ts`
- 新增 `src/types/clocks.ts`
- 扩展 `PromptBuildTrace`，但现有字段兼容

验证：

- `npm run typecheck`
- 前端能正常启动
- 没有任何 prompt 输出变化

### 阶段 2：内容资产索引

目标：角色卡和世界书都能转成统一候选条目，但不立刻接管 prompt。

改动：

- `cardToRagEntries(card)`
- `loreToRagEntries(lorebook)`
- `canonToRagEntries(canonTimeline)`
- `buildContentAssetIndex()`
- 写简单测试或 debug 函数输出条目数

验证：

- NPC 10 张卡都能索引。
- 玩家卡能拆出 POV core。
- 世界书约 40 条能转成 RagEntry。
- NPC 的 system_prompt / post_history_instructions 不进入条目内容。

### 阶段 3：sceneCast 强制 RAG

目标：解决“在场角色没被关键词触发就没资料”的死循环。

改动：

- `entityResolver.ts`
- `ragSelector.ts`
- 先在 writer-adapter 外层把 `sceneCast` 对应 NPC 核心 profile 注入 `相关资料`

验证：

- sceneCast 有“凉宫春日”，userInput 没写春日，仍命中春日核心人设。
- sceneCast 有“阿虚”，阿虚核心人设命中。
- 不在场 NPC 不常驻。

### 阶段 4：世界书 constant 收口

目标：让世界书从“平铺 prompt”变回 RAG。

改动：

- 核心 NPC 条目从 `constant: true` 改为 entity-triggered。
- 地点条目从 `constant: true` 改为 location-triggered。
- 保留最小硬事实、玩家定位、输出协议为 constant。

验证：

- 普通日常 prompt 不再每轮全员 NPC 常驻。
- 当前场景地点仍能命中。
- 原作硬事实仍每轮存在。

### 阶段 5：Storylet + Plot Governor 最小版

目标：从事件类型升级到具体剧情钩子。

改动：

- 新增 `src/data/storylets.ts`，先写 10-15 个入学初期 storylet。
- 新增 `plotGovernor.ts`
- 输出 `PlotDecision`
- prompt 中新增短 `[本轮剧情调度]` 块

验证：

- 入学日 canon 仍优先。
- 高平淡度前先能手动指定 micro storylet。
- activeStorylet 的 ragHints 能强制注入对应角色。

### 阶段 6：平淡度检测

目标：连续流水账时自动提高小冲突、小关系推进、流言、异常苗头权重。

改动：

- 新增 `calculateBlandness(history, state)`
- `PlotGovernor` 读取 `BlandnessReport`
- trace 展示 score/reasons

验证：

- 连续 3 批无状态变化，score 升高。
- 有关系变化或新线索，score 降低。
- score > 60 时优先 micro / relationship。

### 阶段 7：Context Budgeter + Orchestrated Prompt

目标：让 prompt 从“拼字符串”变成“按层预算后的计划”。

改动：

- 新增 `contextBudgeter.ts`
- 新增 `contextOrchestrator.ts`
- 新增 `orchestratedPromptEngine.ts`
- writer-adapter 迁移为接收 `PromptPlan`

验证：

- trace 显示每层预算。
- 玩家厚卡不会整张注入。
- 输出协议只出现一次。
- 文风层和剧情事实分离。

### 阶段 8：Validator + Clocks

目标：长期剧情能被代码维护，LLM 只建议，不越权。

改动：

- `WorldState` 增加 clocks
- `stateApplier` 支持 tickClocks
- `turnValidator` 校验 stateChanges
- storylet 支持 clock gate

验证：

- clock tick 可见。
- clock 满格解锁 storylet。
- LLM 非法状态变化被拒绝并记录 warning。

## 黑客松最小闭环

如果时间非常紧，优先做这 5 件：

```text
1. NPC 角色卡 -> RAG 条目索引。
2. sceneCast 强制激活在场 NPC 人设。
3. 世界书核心 NPC constant 收口。
4. 10 个入学初期 storylet + PlotDecision 短块。
5. 规则版 blandnessScore 影响 storylet 权重。
```

这 5 件完成后，效果通常会比继续堆文风 prompt 更明显：

- 在场角色会稳定像自己。
- 不在场角色不会污染注意力。
- 故事不会一直只写环境和内心。
- 每轮 prompt 更短、更准、更容易 debug。

## 验收清单

### RAG

- sceneCast 有春日但 userInput 没写“春日”，RAG 仍命中 `char_haruhi_core`。
- 玩家卡 description 很长时，最终 prompt 不完整注入全文。
- NPC 卡 system_prompt 不进入正文 prompt。
- 路人身份下不注入超自然真相条目。

### Plot

- 入学日 main_line canon 优先于普通 storylet。
- activeStorylet 有 cooldown。
- storylet 的 fixedFacts 进入 prompt，variableOutcomes 不被写死。
- 玩家行动能影响下轮 storylet 候选权重。

### 平淡度

- 连续 3 批无 relation / flag / clue，`blandnessScore > 60`。
- 出现角色主动回应，分数下降。
- 高平淡度时不继续纯环境描写。

### 预算

- trace 展示每层实际字符数或估算 token。
- 超预算时丢弃低优先级资料，不丢 activeStorylet。
- 输出协议只出现一次。

### 调用

- DeepSeek 官方端点仍发送 `thinking: { type: "disabled" }`。
- 正文写作和 JSON 修复使用不同采样参数。
- LLM 失败不会写入历史。

### 状态

- LLM 不能直接标记未发生的 canon event。
- LLM 不能把玩家改成原作角色。
- clock 不越界。
- 非法 stateChanges 进入 warning。

## 最终判断

这套优化的核心不是把项目改复杂，而是把“谁负责什么”变清楚：

```text
内容资产负责资料。
Plot Governor 负责时机。
Context Budgeter 负责注意力。
Prompt Builder 负责排版。
LLM Call Manager 负责调用。
Parser / Validator 负责把输出变成可信状态。
前端负责阅读体验。
```

做完后，项目会从：

```text
LLM 看一堆混在一起的设定和规则，然后自由续写
```

变成：

```text
系统先选好资料和剧情压力，LLM 在清晰边界里写互动轻小说
```

这是更接近酒馆内容包思路、也更适合黑客松快速调效果的方向。
