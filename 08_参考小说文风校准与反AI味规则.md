# 08. 参考小说文风校准与反 AI 味规则

## 目标

把本地参考 EPUB：

```text
C:\Users\Marigold\Desktop\凉宫春日系列 01 凉宫春日的忧郁.epub
```

转化成项目可执行约束。当前方案已切换为参考 `夏瑾 双鱼座 Beta 0.40.json` 的整体思路：模块化风格 prompt + 输出正则清洗。

本文件只记录文风、节奏、反 AI 味和生成门禁；不复用参考作品的人物、地点、组织、剧情、专有名词或标志性台词。

## 校准结论

参考文本的核心不是华丽辞藻，而是：

- 强第一人称 POV。
- 现场观察经过“我”的判断、误会、吐槽和事后补充过滤。
- 短对白与短旁白交替推进。
- 通过动作、对白、表情和前后矛盾推断他人，不全知读取内心。
- 结尾保留下一拍，不做主题升华。

本地统计口径约为：

```text
段落平均约 35 字
句子平均约 20 字
对白段落接近一半
第一人称密度很高
```

因此点击单元不应是一句一句，也不应是一整屏墙文；默认应把若干短段组合成一个 beat。

## 固定 Prompt 资产

代码位置：

```text
src/lib/prompting/referenceStyleProfile.ts
```

每轮 writer-adapter 生成都会注入一组夏瑾式模块：

- `<核心风格>`
- `<写作指导>`
- `<创作准则>`
- `<段落与对白>`
- `<抗机器人>`

这些块只管“怎么写”，不管剧情事实。现阶段不再把原著摘录作为主要注入，而是用更接近酒馆预设的模块结构直接告诉模型：轻小说、人物驱动、对白信息量、结尾可见行动、稍微冒险、对白分离、抗机器人。

移植边界：

```text
不移植 NSFW 模块。
不移植第三人称规则；本项目继续第一人称。
不移植长思考/最大推理强度；DeepSeek thinking 继续关闭。
不移植正文后状态栏格式；本项目继续 natural -> rule adapter。
```

## 硬禁

出现后应触发硬失败或修复：

- `正文如下`、`以下是`、`作为AI`、`Markdown`。
- `命运的齿轮`、`心跳漏了一拍`、`复杂的情绪`、`微妙的氛围`、`难以言喻`。
- `他不知道的是`、`所有人都没有意识到` 等全知预告。
- `这一刻终于明白`、`青春/人生/世界就是如此` 等升华句。
- `嘴角勾起`、`眼中闪过`、`深吸一口气` 等模板动作连续出现。
- `不是……而是……`、`并非……而是……`、`不只是……更是……`、`不仅……而且/还/也/更……` 等二元解释腔。
- `不是 A。也不是 B。更像是/倒像是……` 等连续否定解释腔。
- 破折号解释腔，尤其是 `——这/那/一种/某种/意味着/也就是/说白了`；同一轮正文里 `——` 过量也触发失败。
- `你决定`、`你相信`、`你意识到`、`你感到` 等替读者做决定的句式。
- `tts_text`、`voice`、`emotion`、`tone`、`stateChanges`、`choices` 等系统字段进入正文。

## 限量词

这些词不是一概禁用，但必须低频，并服务 POV 或具体动作：

```text
仿佛、宛若、一丝、一抹、微微、缓缓、不禁、感到、意识到
```

参考文本里 `好像`、`大概`、`不过`、`反正`、`总之` 等不是 AI 味本身；它们是第一人称判断和吐槽节奏的一部分。限制方式是：

```text
可以用，但必须是“我”的推测、反驳、修正或吐槽，不能变成论文连接词。
```

## 本地 Linter

代码位置：

```text
src/lib/prompting/storyStyleLinter.ts
```

检查维度：

- 禁词和禁句式。
- 连续否定解释腔、抽象心理入口。
- 第一人称 POV 锚点。
- 玩家代理权。
- TTS / 系统字段污染。
- 限量词密度。
- 现场动作、声音、物件、表情锚点。
- beat 过碎或过长。
- 对白比例。
- 结尾闭环升华。

writer-adapter 模式下，linter 仍记录警告和硬失败；展示前另有一层输出清洗，直接处理高频八股残留。

## Prompt 结构原则

当前结构把“生成时的方向”和“事后检查”分开：

```text
生成 prompt：夏瑾式模块化风格与创作准则
输出清洗：直接磨掉高频八股句式和模板边角
本地 linter：记录坏句式、系统字段污染和过长/过碎节奏
```

生成 prompt 的核心不再只写“现场锚点”，而是让模型有叙事驱动力：

```text
轻快俏皮的第一人称轻小说。
大量自然内心独白直接融入叙事。
人物欲望、误会、小麻烦驱动情节。
对白独立成段，和描写穿插交错。
每批以可见行动或需要回应的一拍收尾。
```

这样比单纯列禁令更接近酒馆预设的工作方式：先用风格模块把分布推向正确区域，再用输出清洗处理模型偶尔吐出的模板边角。

## 输出清洗

代码位置：

```text
src/lib/prompting/storyStyleLinter.ts
```

`sanitizeStoryStyleText()` 参考夏瑾预设的正则策略，在自然正文进入点击拆分前执行。当前会处理：

```text
而是 / 不是...解释腔
不是A。也不是B。更像是...
中文破折号
这个动作 / 这个反应 / 这个认知
突然 / 忽然
一丝 / 一抹 / 弧度
不容置疑 / 难以察觉 / 微不可察
手指发白模板
语气像 / 话像 / 如同 / 仿佛 类比喻
```

命中规则会进入右侧调试警告，格式为：

```text
夏瑾式输出清洗：规则名
```

## DeepSeek 设置

代码位置：

```text
src/lib/llmProviders.ts
```

官方 DeepSeek 端点默认会开启 thinking。项目现在在 `api.deepseek.com` 请求体中显式加入：

```json
{"thinking": {"type": "disabled"}}
```

目的：

- 创作请求不走 reasoning。
- `temperature/top_p` 对文风生效。
- 降低等待时间。

## 点击节奏

代码位置：

```text
src/lib/prompting/storyTurnAdapter.ts
```

自然正文会先清理标签，再按自然段/句群切分，并把短段合成 beat：

```text
目标：每个 beat 约 80-220 字
长段会先按约 140 字句群切开
不在适配器里截断正文，由 writer 篇幅控制总量
最后一个 beat 才要求 choices
```

这样能保留“点击推进”的阅读节奏，同时避免每句都变成独立卡片。

## 结构污染兜底

自然正文模式下，如果模型在正文后又补出 `<event_json>`、JSON 代码块或明显的 `stateChanges / choices / worldFlags` 尾巴，先在 `cleanNaturalText()` 中剔除，再进入点击拆分和前端渲染。

这层属于解析兜底，不依赖 prompt 约束；prompt 继续保持“只给正向写法”的结构。
