# 05. 黑客松酒馆套皮与适配难度评估

## 结论先行

如果黑客松时间很紧，我建议不要在当前 React 项目里完整复刻 SillyTavern 的 Prompt Manager。

更现实的路线是二选一：

```text
路线 A：当前 React 游戏继续作为主 demo
        只做“酒馆内容格式适配层”
        目标：能导入/读取预设、角色卡、世界书、正则的核心字段
        难度：中
        黑客松成功率：高

路线 B：直接把游戏 UI 做成 SillyTavern 套皮/扩展
        让 SillyTavern 自己负责角色卡、世界书、预设、正则、生成链路
        目标：酒馆是真运行时，我们只做游戏化展示层
        难度：中高
        黑客松成功率：中
```

如果要“尽量多用轮子”，路线 B 的方向更正确。

但如果只剩一两天，路线 A 更稳；路线 B 更像产品正确方向，但容易被 SillyTavern 前端状态、CSRF、用户目录、扩展事件、聊天上下文这些细节拖住。

我的建议是：

```text
黑客松交付：路线 A
演示话术：路线 B 是下一步，甚至可以做成 SillyTavern 扩展套皮
```

如果评委/队友特别看重“真的复用酒馆”，就改成：

```text
黑客松交付：路线 B 的最小扩展版
牺牲：当前 React 项目的部分 UI 完整度
换取：真正跑在 SillyTavern 运行时上
```

## 我看到的现状

### 当前 React 项目

项目已经有这些东西：

- `src/lib/promptRouter.ts`：自研 prompt 拼装，模拟酒馆 9 步顺序。
- `src/types/character.ts`：已经接近 Character Card V2，支持 `data.character_book` 类型。
- `src/types/lorebook.ts`：世界书类型接近酒馆 Character Book / World Info。
- `src/data/presets/index.ts`：自研 Preset，里面写了大量输出协议、节奏、文风、主线机制。
- `src/lib/cardIO.ts`：目前只支持角色卡 JSON 导入/导出，不支持 PNG 内嵌 metadata。
- `src/lib/llm.ts`：已经有 `<event_json>` 提取、JSON 兜底解析、fallback。
- `src/components/StoryView.tsx`：每轮选择 preset、拼 prompt、调 LLM、解析 event_json、更新状态。
- `src/App.tsx`：世界书目前固定使用 `hsuzumiyaLore`。

这说明项目已经能跑，但核心 AI 编排是“自研小酒馆”，不是直接调用 SillyTavern 运行时。

### 本机 SillyTavern

本机有 `D:\Aheikesong\SillyTavern`。

关键点：

- `package.json` 显示版本 `1.17.0`，Node 要求 `>=20`。
- License 是 `AGPL-3.0`，如果把修改后的 SillyTavern 分发出去，需要注意开源义务。
- 服务端有 `/api/presets`、`/api/worldinfo`、`/api/characters`、`/api/settings`、`/api/backends/chat-completions/generate` 等接口。
- Preset、World Info、Character Card、Regex 的管理链路都很完整。
- 但真正的“按当前角色、世界书、预设、聊天历史组装 prompt 并生成”的主流程主要在 SillyTavern 前端 `public/script.js` 的 `Generate()` 链路里。

这意味着：

```text
酒馆的资源 CRUD 好复用。
酒馆的完整生成运行时，不是一个简单后端 API。
```

所以如果当前 React 外部调用 SillyTavern，只调 `/api/presets`、`/api/worldinfo`、`/api/characters` 是不够的；它只能拿到资源，不能自动得到“像酒馆一样生成”的效果。

## 方案评估

## 方案 A：在当前 React 项目里做酒馆格式适配层

### 做法

保留当前 React 游戏壳和 `StoryView`。

新增一层：

```text
src/lib/sillytavernAdapter.ts
```

它负责把酒馆内容转换成当前项目能用的结构：

- Character Card V2 JSON -> `CharacterCardV2`
- Character Card `data.character_book` -> `Lorebook`
- SillyTavern World Info JSON -> 当前 `Lorebook`
- SillyTavern Chat Completion Preset -> 当前 `Preset` 或 prompt plan
- `extensions.regex_scripts` -> 最小正则后处理

### 难度

```text
总体：中
预计：0.5-2 天可做出可演示版本
完整度：40%-60% 酒馆兼容
黑客松成功率：高
```

### 分项难度

| 模块 | 当前基础 | MVP 难度 | 完整难度 | 说明 |
| --- | --- | --- | --- | --- |
| 角色卡 JSON | 已有 `parseCardJson` | 低 | 中 | V2 JSON 已经能吃，补字段兼容即可 |
| 角色卡 PNG | 暂不支持 | 中 | 中高 | 需要读 PNG tEXt chunk 里的 `chara` / `ccv3` |
| `data.character_book` | 类型已有，router 未合并 | 低 | 中 | 先合并进 `lorebook.entries` 即可 |
| World Info JSON | 类型接近 | 中 | 高 | 酒馆 entries 常见为对象，需要转数组；完整扫描机制很复杂 |
| Preset JSON | 当前格式不同 | 中高 | 高 | `prompts + prompt_order + marker` 需要适配 |
| Regex scripts | 当前只有 event_json 解析 | 中 | 高 | 先做 AI_OUTPUT / WORLD_INFO 两个 placement 就够 demo |
| Prompt 顺序 | 当前自研 9 步 | 中 | 很高 | 完整复刻 Prompt Manager 不建议黑客松做 |

### 优点

- 现有项目能继续跑，风险低。
- 不需要用户启动 SillyTavern。
- 可以快速演示“导入酒馆内容包”的方向。
- 当前 UI、状态、存档、结局、event_json 解析都能保留。

### 缺点

- 这不是“真正跑在酒馆上”，而是“兼容酒馆格式”。
- 外部 Preset 的效果不一定和 SillyTavern 里完全一致。
- Regex、World Info 扫描、Prompt Order 只能做子集。
- 后续如果继续补，会逐渐变成复刻 SillyTavern。

### 黑客松推荐范围

只做这些：

```text
1. 支持导入 Character Card V2 JSON
2. 合并 card.data.character_book
3. 支持导入 World Info JSON，转换 entries
4. 支持导入 OpenAI/Chat Completion preset 的 prompts + prompt_order 核心字段
5. 支持 regex_scripts 的 AI_OUTPUT 最小替换
6. 保留项目自己的最小 <event_json> 契约
```

暂时不要做：

```text
1. 完整 PNG 写回
2. 完整 World Info token budget / recursion / probability / decorators
3. 完整 SillyTavern Prompt Manager
4. 完整 regex placement
5. 完整多用户/多角色/多聊天文件管理
```

## 方案 B：把游戏做成 SillyTavern 套皮/扩展

### 做法

不要让当前 React 项目在外部调用酒馆资源后自己组 prompt。

而是把游戏 UI 放到 SillyTavern 里面，作为一个扩展或定制面板运行：

```text
SillyTavern 原生运行时
  负责：角色卡、世界书、预设、正则、聊天历史、模型连接、生成

游戏套皮扩展
  负责：按钮、状态面板、event_json 渲染、选择项、存档体验
```

也就是说：

```text
酒馆是真引擎
游戏只是它的一层 UI
```

### 难度

```text
总体：中高
预计：1-3 天可做出粗糙 demo，1 周以上才比较稳定
完整度：70%-90% 酒馆兼容
黑客松成功率：中
```

### 为什么不是低难度

因为 SillyTavern 的完整生成链路不只是服务端接口。

它的关键逻辑在前端：

- 当前角色选择
- 当前聊天上下文
- 当前 preset manager
- World Info 扫描
- Regex 启用状态
- Prompt 组合
- `Generate()` 调用
- 生成后写入聊天

这些东西都在 SillyTavern 页面环境里互相依赖。

如果当前 React 项目作为外部页面去调用 SillyTavern，需要解决：

- 同源 / CORS
- CSRF token
- 用户目录和登录态
- 当前角色与聊天状态
- 如何触发 `Generate()`
- 如何拿到生成后的最后一条消息
- 如何避免跟原生聊天 UI 打架

所以真正合理的“套皮”不是外部 React 调它，而是写 SillyTavern 扩展，在同一个页面环境里复用它。

### 最小扩展 demo 怎么做

可以做一个 SillyTavern 扩展：

```text
public/scripts/extensions/haruhi-game/
  manifest.json
  index.js
  style.css
```

扩展做这些事：

1. 在 SillyTavern 页面上挂一个“游戏模式”面板。
2. 读取当前聊天最后一条 AI 消息。
3. 用 `<event_json>([\s\S]*?)</event_json>` 抽 JSON。
4. 渲染 narration / dialogue / choices / stateChanges。
5. 玩家点 choice 后，把选择写入 SillyTavern 输入框。
6. 调用 SillyTavern 原生发送/生成流程。
7. 生成完成后再次解析最新 AI 消息并刷新游戏面板。

这条路最大优点是：

```text
角色卡、世界书、预设、正则真的都由 SillyTavern 管。
```

最大缺点是：

```text
需要熟悉 SillyTavern 扩展 API 和前端事件。
```

### 黑客松推荐范围

只做：

```text
1. 在 SillyTavern 里准备好角色卡 / 世界书 / 预设 / 正则
2. 扩展只解析 event_json 并渲染游戏面板
3. choice 点击后把文本送回酒馆输入
4. 状态暂存在 localStorage 或 event_json 历史中
```

不做：

```text
1. 不替换 SillyTavern 全 UI
2. 不改 SillyTavern 核心 Generate 逻辑
3. 不自己管理 preset/worldinfo/character 文件
4. 不做复杂 React 打包进扩展，先用原生 JS/HTML/CSS
```

## 方案 C：当前 React 外部调用 SillyTavern 作为后端

### 做法

当前 React 项目继续独立运行，但把 SillyTavern 当成本地后端：

- 调 `/api/characters/*` 管角色卡。
- 调 `/api/worldinfo/*` 管世界书。
- 调 `/api/presets/*` 管预设。
- 调 `/api/settings/*` 读设置。
- 调 `/api/backends/chat-completions/generate` 做模型请求。

### 难度

```text
总体：高
预计：2-5 天只够做出半通，稳定性不可控
黑客松成功率：低到中
```

### 关键问题

`/api/backends/chat-completions/generate` 更像“模型供应商请求代理”，不是“酒馆完整生成运行时”。

它不会自动帮你：

- 选择当前角色卡。
- 扫 World Info。
- 按 preset prompt_order 组 messages。
- 跑 regex placement。
- 管聊天历史。
- 插 Author's Note。

这些仍然要你自己做。

所以这条路线看似“调用酒馆后端”，实际上还是回到自研 Prompt Manager。

不推荐黑客松走这条。

## 方案 D：完整复刻 SillyTavern Prompt Manager 到当前 React

### 难度

```text
总体：很高
预计：1-3 周起步
黑客松成功率：低
```

这相当于把 SillyTavern 最复杂、最有价值的部分搬出来重写：

- prompt order
- marker 替换
- world info 扫描
- recursion
- token budget
- examples/depth/outlet/author note 插入
- regex 多 placement
- extension prompts
- 聊天历史压缩
- 多 API preset 差异

这不是黑客松任务。

## 推荐黑客松决策

### 如果目标是“能稳定演示”

选方案 A。

交付口径：

```text
我们已经做了一个 SillyTavern-compatible 游戏壳。
它能消费酒馆式角色卡、世界书、预设、正则的核心字段。
黑客松后会把 runtime 从兼容层替换为 SillyTavern 扩展/套皮。
```

### 如果目标是“必须真的用酒馆轮子”

选方案 B。

交付口径：

```text
我们把游戏做成 SillyTavern 的互动小说套皮。
SillyTavern 继续负责角色卡、世界书、预设、正则和模型生成。
我们的扩展只把输出 JSON 渲染成游戏界面。
```

### 我个人建议

黑客松阶段：

```text
主线做方案 A。
同时准备方案 B 的架构说明和一个极小 proof-of-concept。
```

也就是：

- demo 用当前 React 项目，保证好看、能玩、能存档。
- 内容格式尽量向酒馆靠。
- 文档明确下一步是 SillyTavern 扩展套皮。
- 如果还有时间，再做一个 ST 扩展小面板，证明能在酒馆页面里解析 `<event_json>`。

这是最稳的组合。

## 具体排期建议

### 0-4 小时：锁定演示策略

必须决定：

```text
黑客松 demo 到底跑在哪里？
```

如果跑当前 React：

- 不要碰 SillyTavern 核心。
- 做格式适配。

如果跑 SillyTavern：

- 不要继续扩 React 外部 runtime。
- 做 ST 扩展面板。

不要两个都深做。

### 4-12 小时：当前 React 适配 MVP

优先级：

1. `card.data.character_book` 合并进 promptRouter。
2. 加 World Info JSON import/convert。
3. 加 preset JSON import/convert，只支持最常见 OpenAI chat completion preset。
4. 加 regex AI_OUTPUT 最小处理。
5. UI 上显示“当前加载的卡 / 世界书 / 预设 / 正则”。

### 12-24 小时：内容打磨

1. 做一张作品入口卡。
2. 做一份世界书。
3. 做一份 preset。
4. 做一个 event_json regex。
5. 用 2-3 个模型测试稳定输出。

### 24-36 小时：如果要套皮 proof-of-concept

1. 在 SillyTavern 里手动导入卡、世界书、预设、正则。
2. 新建一个扩展面板。
3. 只做“读取最后一条消息 -> 解析 event_json -> 渲染 narration/choices”。
4. choice 点击后先允许手动复制到输入框。
5. 有余力再自动填入输入框并触发发送。

## 最终推荐文案

可以这样对队友说：

```text
我们不应该继续造一个完整的小酒馆。

黑客松里，为了能稳定演示，当前 React 游戏先保留为主壳；
但是 LLM 内容资产全部按 SillyTavern 的角色卡、世界书、预设、正则格式组织。

短期我们做“格式适配层”，让项目能吃这些资产。
中期我们把游戏 UI 做成 SillyTavern 扩展，让 SillyTavern 真正成为 runtime。

这样既不浪费现有 demo，又不会长期陷入复刻酒馆 Prompt Manager 的坑。
```

## 一句话判断

```text
适配酒馆格式：黑客松可做，推荐。
直接套 SillyTavern：方向正确，但要做成扩展，不要外部硬调；黑客松只能做最小面板。
完整复刻酒馆运行时：不适合黑客松。
```

