# 02. 前端样式设计

## 设计定位

前端目标不是做完整 galgame，而是做一个 **好读、好点、有主题感的互动小说界面**。

关键词：

- 校园异常
- 社团活动室
- 放学后
- 轻量视觉小说
- 文字冒险
- 清爽、现代、可演示

视觉上可以借一点 galgame 的语言，但不要承诺复杂立绘和 CG。

## 参考方向

可以参考：

- AI Dungeon：自由输入式文字冒险。
- Fallen London：事件卡、选择、状态推进。
- 轻量视觉小说：场景名、角色名、对白框、氛围背景。

不参考：

- 纯聊天窗口。
- 后台管理系统。
- 大片表格和表单。
- 古风卷轴。
- 完整商业 galgame 的复杂资产系统。

## 第一屏布局

打开页面后直接进入主界面，不做营销首页。

建议布局：

```text
┌──────────────────────────────────────────────┐
│ 顶部栏：标题 / 章节 / 当前场景 / 异常等级       │
├──────────────────────────────┬───────────────┤
│                              │ 状态面板       │
│ 当前事件卡                    │ - 时间         │
│ 剧情文本                      │ - 地点         │
│ 对白                          │ - 异常等级     │
│ 历史摘要                      │ - 线索         │
│                              │ - 成员状态     │
├──────────────────────────────┴───────────────┤
│ 选项按钮 1 / 选项按钮 2 / 选项按钮 3            │
│ 自由输入框：输入自己的行动                      │
└──────────────────────────────────────────────┘
```

移动端可以改成：

```text
顶部栏
事件卡
剧情文本
状态折叠面板
选项按钮
自由输入框
```

## 核心组件视觉

### 事件卡

用途：显示当前事件标题和氛围。

建议：

- 半透明卡片或柔和实色卡片。
- 标题醒目，但不要巨大。
- 有小标签：场景、时间、氛围。
- 卡片边缘可以有轻微发光或异常纹理。

不要：

- 大段说明文字。
- 像新闻列表。
- 像后台通知框。

### 剧情阅读区

用途：显示旁白和事件推进。

建议：

- 行距舒服。
- 每轮事件分块展示。
- 当前轮淡入。
- 历史轮次可以略微降低亮度。
- 旁白和对白区分清楚。

### 对白

用途：显示角色说话。

没有立绘时，可以用：

- 角色名标签。
- 小头像色块。
- 心情 badge。
- 左侧竖线区分不同角色。

示例：

```text
团长 / 兴奋
「我宣布，今天的活动是寻找真正的异常现象！」
```

### 选项区

用途：给玩家固定行动。

建议：

- 2 到 4 个按钮。
- 按钮是完整行动句。
- hover 时轻微位移或高亮。
- 选择后按钮短暂禁用，显示生成中。

不要：

- 太多选项。
- 按钮文字过长挤爆。
- 像普通表单提交按钮。

### 自由输入框

用途：支持 AI Dungeon 式自由行动。

建议文案：

```text
输入你的行动，例如：我检查旧电脑的系统日志
```

按钮文案：

```text
执行行动
```

交互：

- Enter 提交。
- Shift + Enter 换行。
- 空输入不提交。
- 生成中禁用。

### 状态面板

用途：让用户看到故事不是纯聊天，而是有状态。

显示：

- 当前地点
- 当前时间
- 异常等级
- 玩家压力
- 已获得线索
- 成员状态

异常等级可以做成进度条：

```text
异常等级 32 / 100
██████░░░░
```

成员状态可以做成列表：

```text
团长：兴奋
文静少女：观察中
学姐：紧张
转学生：微笑
```

## 色彩方向

建议色彩：

- 背景：浅米白、冷灰蓝、黄昏橙、教室木色。
- 强调色：红色袖章感、黄色发带感、蓝白校服感。
- 异常色：偏紫、偏青、轻微霓虹，但少量使用。

建议 CSS 变量：

```css
:root {
  --bg: #f5f1e8;
  --panel: rgba(255, 255, 255, 0.82);
  --text: #20242c;
  --muted: #6b7280;
  --line: rgba(32, 36, 44, 0.12);
  --accent: #d33b32;
  --accent-2: #f2c94c;
  --school-blue: #3d6fb6;
  --anomaly: #7c5cff;
}
```

避免：

- 大面积深色后台风。
- 大面积古风黄褐。
- 大面积纯紫渐变。
- 太像默认 Tailwind demo。

## 字体

建议：

- 中文正文：系统 sans-serif 或思源黑体类。
- 标题：稍微有轻小说感，但不要花。
- 不使用太多字体。

CSS：

```css
font-family:
  "Noto Sans SC",
  "Microsoft YaHei",
  system-ui,
  sans-serif;
```

## 动效

只做轻量动效：

- 当前事件淡入。
- 选项 hover。
- 异常等级变化时闪一下。
- 生成中 loading dots。
- 新线索出现时轻微高亮。

不要做：

- 大量粒子。
- 复杂转场。
- 每个元素都动画。
- 影响阅读的闪烁。

## 美术资源策略

两天 MVP 不依赖真实立绘。

替代方案：

- 角色名卡。
- 色块头像。
- 简单剪影。
- CSS 做的社团活动室背景氛围。
- 模糊校园背景图。
- 后续再加立绘。

优先级：

1. 页面布局和阅读体验。
2. 交互清晰。
3. 主题色和状态面板。
4. 背景氛围。
5. 立绘和 CG。

## 文案风格

界面文案要像游戏，不像工具。

推荐：

- 当前事件
- 已发现线索
- 异常等级
- 执行行动
- 继续观察
- 生成中

避免：

- Submit
- Response
- JSON Output
- API Result
- Debug Panel

调试信息可以隐藏在开发模式里。

## 最小可展示页面

第一版只要做好这几个区域：

- 顶部标题栏
- 当前事件卡
- 剧情文本
- 角色对白
- 选项按钮
- 自由输入框
- 右侧状态面板

即使没有立绘，也必须让评委一眼看出：

> 这是一个可以玩的 AI 互动故事，而不是普通聊天框。
