# 06. 酒馆预设 80% 相似度适配施工计划

## 目标

把当前项目从“自研 Preset 主导输出效果”，改成：

```text
SillyTavern Chat Completion preset 主导文风、写作方式、prompt 顺序、采样参数。
项目只保留游戏壳、状态更新、最小 event_json 接口契约。
```

目标不是 100% 复刻 SillyTavern，而是在黑客松时间内做到：

```text
80% 像酒馆里使用同一个预设时的输出效果。
```

这里的“80% 像”定义为：

- 按酒馆 preset 的 `prompt_order` 排列启用 prompt。
- 保留 preset 内 prompt 的 `role`、`content`、大部分顺序语义。
- 替换主要 marker：`charDescription`、`charPersonality`、`scenario`、`worldInfoBefore`、`worldInfoAfter`、`dialogueExamples`、`chatHistory`、`personaDescription`。
- 使用 preset 的采样参数。
- 当前项目的大段文风/节奏/禁止事项 prompt 不再参与主导。
- 最后只追加一个极短的 `<event_json>` 输出契约，保证前端能解析。

## 非目标

这些明确不做，避免黑客松陷入复刻酒馆：

- 不完整移植 SillyTavern Prompt Manager。
- 不完整实现 World Info 递归扫描、token budget、概率触发、decorators、depth/outlet 等高级机制。
- 不完整实现 regex engine。
- 不读取 PNG 角色卡 metadata。
- 不改 LLM provider 层。
- 不重写 UI、存档、世界状态、结局系统。
- 不删除旧 preset 代码；只在新模式下旁路它。

## 强制边界

### 必须保留

这些是游戏壳价值，不动或只做最小接线：

- `src/components/StoryView.tsx` 的主循环：用户输入 -> LLM -> `parseEventJson` -> `applyTurn` -> UI 更新。
- `src/lib/llm.ts` 的 `runLLM`、`parseEventJson`、fallback。
- `src/lib/llmProviders.ts` 的 OpenAI/Anthropic/本地兼容调用。
- `src/lib/worldState.ts`、`src/lib/timeAdvance.ts`、`src/lib/storage.ts`。
- `src/types/turn.ts` 的 `StoryTurn` 输出协议。
- 当前 React UI 组件。

### 必须降权或旁路

这些会和酒馆 preset 抢控制权：

- `src/data/presets/index.ts` 的大段文风、节奏、输出协议。
- `promptRouter.mergeSystemPrompts(preset.system_prompt, cardData.system_prompt)` 里的角色卡 `system_prompt` 合并。
- `cardData.post_history_instructions` 里的输出规则。
- `hsuzumiya_lore` 中偏文风/禁止事项/输出协议的条目。
- `randomMode.ts` 生成角色卡时写入的 `system_prompt` / `post_history_instructions`，在新 preset 模式下不应继续参与主 prompt。

注意：这里说“降权或旁路”，不是删除。旧逻辑保留为 fallback。

## 总体架构

新增一层独立 prompt engine，不把酒馆 preset 逻辑塞进旧 `promptRouter.ts`。

```text
StoryView
  -> buildPromptForTurn()
       -> legacyPromptEngine
            使用旧 promptRouter
       -> sillyTavernPresetEngine
            使用 ST preset compiler
  -> runLLM(messages, sampling)
  -> parseEventJson(raw)
```

建议目录：

```text
src/lib/prompting/
  index.ts
  legacyPromptEngine.ts
  outputContract.ts
  runtimeContext.ts
  conflictPolicy.ts

src/lib/sillytavern/
  presetTypes.ts
  presetParser.ts
  presetCompiler.ts
  markerResolver.ts
  samplingMapper.ts
  presetFixtures.ts
```

设计原则：

- `sillytavern/*` 只理解酒馆 preset 格式，不依赖 React。
- `prompting/*` 负责把项目的 card/lore/state/history 转成编译 preset 所需的上下文。
- `StoryView` 只知道调用 `buildPromptForTurn()`，不知道内部是旧模式还是酒馆模式。

## 需要剔除的冲突源

### 1. 当前内置 Preset

冲突位置：

```text
src/data/presets/index.ts
```

问题：

- 它写了大量文风、节奏、输出格式、主线机制。
- 酒馆 preset 也在写这些。
- 两者同时启用，会形成“双上级命令”。

施工方式：

- 保留文件。
- 新增 `legacy` 模式继续使用它。
- 新增 `sillytavern-preset` 模式完全绕开 `presetRegistry` 和 `chainPresets`。
- 当前 `pickPreset()` 不删，但只由 `legacyPromptEngine` 使用。

验证边界：

- 新模式编译出来的 messages 中不应包含 `[Preset · System Prompt]`。
- 新模式 messages 中不应出现当前 `outputProtocol` 里的大段“主线机制 / 身份变化 / 节奏字段”。
- 旧模式仍能正常 mock 跑通。

### 2. 角色卡 system_prompt / post_history_instructions

冲突位置：

```text
src/lib/promptRouter.ts
src/lib/randomMode.ts
```

问题：

- 生成角色卡时会要求 `system_prompt` 写 POV 规则。
- `post_history_instructions` 会写 `<event_json>`。
- 酒馆 preset 也可能已经写了写作方式、视角、格式强化。

施工方式：

新模式 marker 替换时只使用：

```text
card.data.description
card.data.personality
card.data.scenario
card.data.mes_example
```

默认不注入：

```text
card.data.system_prompt
card.data.post_history_instructions
```

如果后续需要，可以加开关：

```ts
includeCharacterSystemPrompt: false
includePostHistoryInstructions: false
```

验证边界：

- 新模式 messages 中不应出现 `[Character system_prompt]`。
- 新模式 messages 中不应出现随机卡生成阶段写入的“回复必须以 <event_json> 块结尾”长句。
- 仍能在 marker `charDescription` 处看到角色 description。

### 3. 世界书中的文风/格式条目

冲突位置：

```text
src/data/lorebooks/hsuzumiya_lore.ts
```

问题条目类型：

- `原作叙事风味`
- `禁止事项`
- 含“回复结尾必须只有一个 <event_json>...”的条目

施工方式：

先不大规模编辑世界书，新增冲突过滤层：

```text
src/lib/prompting/conflictPolicy.ts
```

提供：

```ts
filterLoreForSillyTavernPresetMode(entries): entries
```

第一版按条目 `name` 过滤：

```text
过滤：原作叙事风味
过滤：禁止事项
保留：原著硬事实、地点、人物、组织、事件链、时间线
```

以后再把世界书拆成：

```text
facts
style
output_contract
```

验证边界：

- 新模式 trace 显示被注入的 lore entry 不包含 `原作叙事风味`、`禁止事项`。
- 原著硬事实、北高、SOS 团、人物条目仍可注入。
- 旧模式不受影响。

### 4. Author's Note 模板

冲突位置：

```text
src/lib/promptRouter.ts -> renderAuthorsNote()
src/data/presets/index.ts -> authors_note_template
```

问题：

- 当前 Author's Note 模板属于自研 preset 的一部分。
- 新模式如果继续用它，会把旧 preset 的节奏/身份规则带回来。

施工方式：

新模式不调用 `renderAuthorsNote()`。

改为生成“运行时世界信息块”：

```text
当前日期
当前身份
当前状态摘要
当前事件链
最近线索/flags
时间表窗口
身份边界摘要
```

这块不作为 preset，而作为：

```text
worldInfoAfter 附加内容
```

也就是说它走 marker，不走旧 Author's Note。

验证边界：

- 新模式 messages 中不应出现 `[Author's Note]` 旧标题。
- 新模式 `worldInfoAfter` 内容中能看到当前日期、身份、flags、timeline。

## 新模块设计

## 1. `presetTypes.ts`

职责：

只描述酒馆 preset JSON 的宽松类型。

必须宽松，因为社区 preset 字段变化大。

核心类型：

```ts
export type SillyTavernPrompt = {
  identifier?: string;
  name?: string;
  role?: "system" | "user" | "assistant" | "model";
  content?: string;
  marker?: boolean;
  system_prompt?: boolean;
  injection_position?: number | string;
  injection_depth?: number;
  forbid_overrides?: boolean;
  injection_order?: number;
  injection_trigger?: string;
};

export type SillyTavernPromptOrderItem = {
  identifier: string;
  enabled: boolean;
};

export type SillyTavernPromptOrder = {
  character_id?: number;
  order: SillyTavernPromptOrderItem[];
};

export type SillyTavernChatCompletionPreset = {
  prompts?: SillyTavernPrompt[];
  prompt_order?: SillyTavernPromptOrder[];
  temperature?: number;
  top_p?: number;
  top_k?: number;
  frequency_penalty?: number;
  presence_penalty?: number;
  openai_max_tokens?: number;
  extensions?: Record<string, unknown>;
  [key: string]: unknown;
};
```

边界：

- 不做运行逻辑。
- 不 import 项目 card/lore/state。

验证：

- `npm run typecheck`。

## 2. `presetParser.ts`

职责：

解析 JSON 文本，做最小校验。

输入：

```ts
parseSillyTavernPresetJson(text: string)
```

输出：

```ts
{
  preset: SillyTavernChatCompletionPreset;
  diagnostics: string[];
}
```

校验规则：

- 必须能 JSON.parse。
- `prompts` 必须是数组，否则报 warning。
- `prompt_order` 必须是数组，否则报 warning。
- prompt 没有 `identifier` 的，不能参与 order 匹配，报 warning。
- 不因为未知字段失败。

边界：

- 不替换 marker。
- 不处理 sampling。
- 不碰 localStorage/UI。

验证：

- 用 `夏瑾 双鱼座 Beta 0.40.json` 解析，得到：
  - `prompts.length === 140`
  - `prompt_order.length === 2`
  - `extensions.regex_scripts.length === 9`

## 3. `samplingMapper.ts`

职责：

把 ST preset 采样参数映射到当前 `SamplingParams`。

映射：

```text
temperature -> temperature
top_p -> top_p
top_k -> top_k
frequency_penalty -> frequency_penalty
presence_penalty -> presence_penalty
openai_max_tokens -> max_tokens
```

安全边界：

- `max_tokens` 要 clamp，不能直接让 32000 打爆所有模型。
- 建议默认上限：

```ts
maxTokensCap = 4000
```

但在 UI 里可以显示“原 preset openai_max_tokens=32000，当前为了稳定演示限制为 4000”。

验证：

- 输入该 preset，输出：
  - `temperature: 1`
  - `top_p: 1`
  - `max_tokens: 4000` 或配置上限

## 4. `markerResolver.ts`

职责：

把酒馆 marker 替换成当前项目内容。

输入：

```ts
resolveMarker(identifier, context): string
```

第一版支持：

| marker | 替换内容 |
| --- | --- |
| `charDescription` | `card.data.description` |
| `charPersonality` | `card.data.personality` |
| `scenario` | `card.data.scenario` |
| `dialogueExamples` | `card.data.mes_example` |
| `chatHistory` | 将 `history` 转成可读聊天文本 |
| `worldInfoBefore` | 选中的 before lore entries |
| `worldInfoAfter` | 选中的 after lore entries + runtime world info |
| `personaDescription` | 当前玩家卡简介，可先用 description 前几行 |

明确不支持但不报错：

```text
enhanceDefinitions
main
jailbreak
nsfw
自定义 uuid prompt
```

这些不是 marker，而是普通 prompt，按 `content` 原样进入。

边界：

- 不做 World Info 关键词扫描；扫描仍由现有 `selectLoreEntries` 负责。
- 不解析酒馆宏。
- 不执行任何 preset 内容。

验证：

- 编译后 messages 中不应出现空 marker 占位。
- `charDescription` 对应位置出现当前角色卡 description。
- `chatHistory` 能包含最近用户输入和 AI 输出。

## 5. `runtimeContext.ts`

职责：

把项目当前 state/timeline/identity 转成“动态世界信息”。

输出：

```ts
renderRuntimeWorldInfo(context): string
```

内容只写事实，不写文风：

```text
[Game Runtime State]
日期：...
身份：...
当前事件链：...
线索：...
flags：...
关系：...

[Timeline Window]
...

[Identity Boundary]
...
```

边界：

- 不写“你应该怎么写”。
- 不写“必须输出 JSON”。
- 不写“narration 控制多少字”。

验证：

- 输出只包含状态事实。
- 没有“文风”“节奏”“禁止事项”等词。

## 6. `outputContract.ts`

职责：

提供唯一硬协议。

内容必须短：

```text
本应用只读取一个 <event_json>...</event_json> 块。
块内必须是合法 JSON。
字段至少包括 eventTitle、narration、stateChanges、pace、timeAdvance、requiresChoice、choices。
块外不要输出其他文字。
```

边界：

- 不写文风。
- 不写剧情节奏。
- 不写角色边界。
- 不写世界设定。

验证：

- 新模式最终 messages 中只出现一次 `<event_json>` 输出契约。
- 旧的长 `outputProtocol` 不出现。

## 7. `presetCompiler.ts`

职责：

核心模块。把 ST preset + 项目上下文编译成：

```ts
{
  messages: ChatMessage[];
  sampling: SamplingParams;
  trace: {
    mode: "sillytavern-preset";
    promptOrderCharacterId?: number;
    enabledPromptCount: number;
    markerHits: string[];
    skippedPrompts: string[];
    warnings: string[];
  };
}
```

算法：

1. 建立 `promptByIdentifier`。
2. 选择 `prompt_order`。
3. 遍历启用 order item。
4. 找对应 prompt。
5. 如果 prompt 是 marker，调用 `resolveMarker()`。
6. 如果 prompt 是普通 prompt，使用 `prompt.content`。
7. 空内容跳过。
8. role 映射：

```text
system -> system
user -> user
model -> assistant
assistant -> assistant
其他 -> user
```

9. 合并相邻同 role 消息，减少碎片。
10. 追加最小 output contract。
11. 映射 sampling。

`prompt_order` 选择策略：

第一版：

```text
优先使用 enabled 数最多的 order。
如果配置了 preferredCharacterId，则用指定 character_id。
```

对这个文件，通常会选中 `character_id: 100001`，因为它启用 prompt 更多，更接近完整写作预设。

边界：

- 不修改原 preset。
- 不处理 regex。
- 不处理 injection_depth。
- 不处理 system squash。
- 不做 token 裁剪，第一版交给模型上下文承受；若超长，后续加裁剪。

验证：

- 用给定 preset 编译时：
  - enabled prompt 约 32 条。
  - trace 能显示选择了哪个 `character_id`。
  - messages 总数不为 0。
  - messages 中包含该 preset 的普通 prompt 内容。
  - messages 中包含 card description / world info / chat history。
  - messages 最后一条或最后一组 system 包含短 output contract。

## 8. `index.ts` / `buildPromptForTurn()`

职责：

统一入口，替换 `StoryView` 直接调用 `assemblePrompt()`。

接口：

```ts
buildPromptForTurn({
  mode,
  stPreset,
  card,
  lorebook,
  state,
  history,
  summary,
  userInput,
  sceneCast,
  timelineContext,
  identityGuide,
  eventKind,
})
```

模式：

```text
legacy
sillytavern-preset
```

边界：

- `StoryView` 只改一处调用。
- 旧模式完全走原 `assemblePrompt()`。
- 新模式走 `compileSillyTavernPresetPrompt()`。

验证：

- `mode=legacy` 时 trace 与旧逻辑一致。
- `mode=sillytavern-preset` 时不调用 `pickPreset()`。

## 实施步骤

## 第 0 步：建立基线

目的：

确认当前项目在改动前的状态。

操作：

```bash
npm run typecheck
npm run build
```

如果当前已有错误：

- 记录下来。
- 不在本任务里顺手修无关错误。
- 后续验证只比较是否新增错误。

验收：

- 有一份基线记录。
- 确认当前 mock 模式能跑开场。

## 第 1 步：新增类型与解析器

新增：

```text
src/lib/sillytavern/presetTypes.ts
src/lib/sillytavern/presetParser.ts
```

不改任何现有业务文件。

验收：

- `npm run typecheck` 通过或不新增错误。
- 用 Node/临时脚本解析 `夏瑾 双鱼座 Beta 0.40.json`，能读到：

```text
prompts: 140
prompt_order: 2
regex_scripts: 9
temperature: 1
top_p: 1
openai_max_tokens: 32000
```

回滚边界：

- 删除新增两个文件即可，不影响现有项目。

## 第 2 步：新增采样映射与输出契约

新增：

```text
src/lib/sillytavern/samplingMapper.ts
src/lib/prompting/outputContract.ts
```

不接入 StoryView。

验收：

- 给定 preset 映射出 sampling。
- `max_tokens` 被上限保护。
- output contract 文本短于 300 字。

回滚边界：

- 删除新增文件即可。

## 第 3 步：新增 marker resolver 与 runtime context

新增：

```text
src/lib/sillytavern/markerResolver.ts
src/lib/prompting/runtimeContext.ts
src/lib/prompting/conflictPolicy.ts
```

实现：

- marker 替换。
- runtime world info。
- lore 冲突过滤。

验收：

- 使用一个假的 card/history/lore/state 调 resolver。
- `charDescription` 返回 description。
- `worldInfoAfter` 包含 runtime state。
- 被过滤 lore 不进入新模式。

回滚边界：

- 不接主流程，删除即可。

## 第 4 步：新增 preset compiler

新增：

```text
src/lib/sillytavern/presetCompiler.ts
```

实现：

- prompt_order 选择。
- marker 替换。
- role 映射。
- 相邻同 role 合并。
- output contract 追加。
- sampling 映射。
- trace。

验收：

使用给定 preset + 假数据编译：

```text
messages.length > 0
trace.enabledPromptCount > 20
trace.markerHits 包含 charDescription / worldInfoBefore / chatHistory
trace.promptOrderCharacterId 有值
sampling.temperature === 1
sampling.top_p === 1
sampling.max_tokens <= 4000
```

人工检查：

- messages 中不出现旧 `[Preset · System Prompt]`。
- messages 中不出现旧大段节奏规则。
- messages 中出现该 preset 的写作 prompt。

回滚边界：

- 仍未接 StoryView，删除新增文件即可。

## 第 5 步：新增统一 prompt engine 入口

新增：

```text
src/lib/prompting/legacyPromptEngine.ts
src/lib/prompting/index.ts
```

`legacyPromptEngine` 只是包装旧：

```ts
pickPreset()
assemblePrompt()
```

`index.ts` 根据 mode 分流。

验收：

- `legacy` 调用结果与旧 `assemblePrompt()` 基本一致。
- `sillytavern-preset` 调用结果来自 compiler。
- typecheck 通过。

回滚边界：

- 尚未改 UI 主流程时删除新增文件即可。

## 第 6 步：最小改 StoryView 接线

修改：

```text
src/components/StoryView.tsx
```

最小改动：

从：

```ts
const preset = pickPreset(...)
const assembled = assemblePrompt(...)
const { raw, turn } = await runLLM({ messages: assembled.messages, sampling: preset.sampling })
```

改成：

```ts
const built = buildPromptForTurn(...)
const { raw, turn } = await runLLM({ messages: built.messages, sampling: built.sampling })
```

新增状态：

```ts
promptMode: "legacy" | "sillytavern-preset"
activeStPreset: SillyTavernChatCompletionPreset | null
```

第一版可以先写死：

```text
legacy
```

然后下一步再加 UI 开关。

验收：

- 默认 legacy 模式行为不变。
- mock LLM 开场仍能跑。
- trace 仍显示 preset/lore 信息。

回滚边界：

- 这一处是主流程风险点。
- 如果失败，恢复 StoryView 原来的 `pickPreset + assemblePrompt` 调用即可。

## 第 7 步：预设加载方式

黑客松推荐两种方式，选一种，不要都做。

### 方式 A：文件上传

改：

```text
src/components/SettingsModal.tsx
```

加一个 JSON 文件选择：

```text
导入 SillyTavern Preset
```

保存到 localStorage。

优点：

- 不把私人 preset 放进仓库。
- 最小侵入。

缺点：

- 每台演示机器要导入一次。

### 方式 B：内置 public preset

新增：

```text
public/presets/夏瑾 双鱼座 Beta 0.40.json
```

启动时 fetch。

优点：

- 演示稳定。

缺点：

- 文件进入项目，需要确认是否适合提交。

推荐：

```text
黑客松演示用方式 B。
正式整理用方式 A。
```

验收：

- UI 能显示：

```text
已加载 ST Preset：夏瑾 双鱼座 Beta 0.40
prompts: 140
prompt_order: 2
regex_scripts: 9
```

## 第 8 步：启用新模式并剔除冲突

启用条件：

```text
activeStPreset != null && promptMode === "sillytavern-preset"
```

新模式必须满足：

- 不调用 `pickPreset()`。
- 不使用 `src/data/presets/index.ts`。
- 不注入 `card.data.system_prompt`。
- 不注入 `card.data.post_history_instructions`。
- 过滤世界书文风/格式条目。
- 只追加短 output contract。

验收：

开发 trace 面板显示：

```text
Mode: sillytavern-preset
ST prompt_order: 100001
Enabled prompts: 32
Marker hits: charDescription, charPersonality, worldInfoBefore, worldInfoAfter, scenario, dialogueExamples, chatHistory
Filtered lore: 原作叙事风味, 禁止事项
```

人工检查 compiled messages：

- 旧大段 `outputProtocol` 不出现。
- 旧 Author's Note 标题不出现。
- 新 preset 内容出现。
- event_json 契约只出现一次。

## 第 9 步：最小真实模型验证

不要做过度测试。只做足够证明“像”和“不坏”。

### 验证 1：编译验证

不调用 LLM，只检查 prompt 编译。

通过条件：

- messages 可生成。
- sampling 合理。
- trace 完整。
- 没有明显旧 prompt 冲突。

### 验证 2：mock 回归

使用 legacy + mock。

通过条件：

- 开场仍可生成。
- 点击继续仍可推进。
- 存档不报错。

### 验证 3：真实 LLM 单轮

使用新模式 + 真实模型。

输入：

```text
（故事开始。请自然展开本轮事件。）
```

通过条件：

- 返回内容能被 `parseEventJson()` 解析。
- UI 能展示 narration/choices。
- fallback 不触发。

### 验证 4：真实 LLM 三轮

做三轮：

1. 开场。
2. 点击一个 choice。
3. 继续推进。

通过条件：

- 3 轮中至少 2 轮 parse 成功。
- stateChanges 不导致 UI 崩。
- 文风明显来自该 preset，而不是旧项目 preset。

### 验证 5：冲突检查

人工抽查 prompt trace。

通过条件：

- 没有旧 preset 大段节奏规则。
- 没有世界书中的“输出必须...”重复命令。
- 没有角色卡 post instructions 重复命令。

## 失败回退策略

### 如果新 preset 编译过长

处理：

- 暂时只取 `enabled` prompt 的前 20 条。
- 或者只取：

```text
jailbreak
nsfw
enhanceDefinitions
写作指南
错误避免
worldInfoBefore
charDescription
charPersonality
worldInfoAfter
scenario
dialogueExamples
chatHistory
```

验收：

- 仍能跑通。

### 如果 event_json 经常失败

处理顺序：

1. 确认短 output contract 是否在最后。
2. 把 output contract role 改为 `system`。
3. 如果还失败，增加一次“修复 JSON”重试，不改主 prompt。

不要：

- 不把旧大段 outputProtocol 加回来。

### 如果文风不像

检查：

- 是否选中了完整 prompt_order。
- 是否只用了采样参数而没用 prompt_order。
- 是否旧 preset 仍混入。
- 是否项目世界书文风条目还在抢控制。

### 如果旧模式坏了

立即回滚 `StoryView` 的接线。

原则：

```text
legacy 必须永远能作为安全 fallback。
```

## 最小文件变更清单

第一阶段新增文件：

```text
src/lib/sillytavern/presetTypes.ts
src/lib/sillytavern/presetParser.ts
src/lib/sillytavern/samplingMapper.ts
src/lib/sillytavern/markerResolver.ts
src/lib/sillytavern/presetCompiler.ts
src/lib/prompting/outputContract.ts
src/lib/prompting/runtimeContext.ts
src/lib/prompting/conflictPolicy.ts
src/lib/prompting/legacyPromptEngine.ts
src/lib/prompting/index.ts
```

第一阶段修改文件：

```text
src/components/StoryView.tsx
src/components/SettingsModal.tsx
src/components/StatusPanel.tsx
```

可选新增：

```text
public/presets/夏瑾 双鱼座 Beta 0.40.json
```

不应修改：

```text
src/lib/llm.ts
src/lib/llmProviders.ts
src/lib/worldState.ts
src/lib/timeAdvance.ts
src/types/turn.ts
src/data/presets/index.ts
src/data/lorebooks/hsuzumiya_lore.ts
```

其中 `src/data/presets/index.ts` 和 `src/data/lorebooks/hsuzumiya_lore.ts` 不是不重要，而是为了最小改动，第一阶段只旁路/过滤，不直接重写。

## 最终验收标准

工程验收：

```text
npm run typecheck 通过或不新增错误
npm run build 通过或不新增错误
legacy 模式可用
sillytavern-preset 模式可用
```

功能验收：

```text
能加载 夏瑾 双鱼座 Beta 0.40.json
能显示 prompt/prompt_order/regex 数量
能按 prompt_order 编译 messages
能替换核心 marker
能使用 preset sampling
能完成至少 3 轮真实模型互动
event_json parse 成功率可接受
```

相似度验收：

```text
旧项目文风规则不再主导
输出风格明显更接近该酒馆 preset
prompt trace 能说明最终 messages 的来源
```

安全验收：

```text
不执行 preset 内容中的任何代码
不执行 regex_scripts
不把本地绝对路径写死进前端运行逻辑
不破坏旧模式
```

## 一句话施工原则

```text
新建 SillyTavern preset compiler，最小接入 StoryView；
旧 promptRouter 留作 fallback；
冲突 prompt 不删除，只在新模式旁路；
每一步都能 typecheck、能编译、能单独回滚。
```

